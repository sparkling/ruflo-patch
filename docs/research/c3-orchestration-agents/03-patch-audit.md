# C3 Orchestration & Agents — Patch-History Audit

**Protocol:** ADR-0292 step 5. For each fork ADR/patch touching the 7 C3 plugins' surfaces, classify the **problem statement** as DEMONSTRATED (evidence of the failure in the ADR / linked artifacts / re-confirmed in the C3 fork env) vs ASSUMED (asserted brokenness without production-shape proof). Plus the mistake catalogue (ADR-0291 §Confirmation taxonomy + C2's "demonstrated-then-but-diverged-since" class).
**Author:** fork-auditor (queen-led C3 swarm). **Date:** 2026-06-04.

> **DA review (2026-06-04) — 23/23 premise audit UPHELD (4 rows sampled: 0098, 0115, 0254, 0290; both LOAD-BEARING-CORRECT upgrades justified).** Errata carried from 02: anywhere this file repeats "0 references in fork HEAD" for `hooks_task-completed`/`teammate-idle`/`agent_logs`, the corrected fact is "0 references in `hooks-tools.ts`, but `commands/hooks.ts` + the hive-mind-advanced SKILL ship as internal consumers — the CLI subcommand no-ops on training" (DA-verified at HEAD). The M-C4/W-1 wasm-prompt premise is corrected the other way: the auditor's captures crashed before seeing an echo; the DA captured the MCP-path echo WITHOUT the documented no-key NOTE (dist `ruvector/agent-wasm.js:117` appends it only on the CLI path) — W-1 is a real minor honesty gap. New M-row added by DA (graded in 04-dispositions): the missing `managed_agent_*` cloud runtime (upstream `ruflo-agent` plugin rename + managed-agent skill, `ef73a1616`/`3975ab512` — neither in the ledger; ledger row 125 covers only the version-bump/README half).

**Corpus sweep:** the C3 surface spans the hive-mind cluster (ADR-0104–0132 + 0138–0145 + 0184/0185, ~26 ADRs), swarm/coordination (0098, 0105, 0114), wasm/rvagent (0254, 0256, 0258, 0259, 0266), hooks (0202, 0203, 0211), the 7 plugin contract ADRs, and the cross-cutting honesty ADRs (0210, 0238, 0239, 0244). This audit **deep-classifies the divergence-relevant keystones + the top regression's provenance**; the large README-claim-validation hive cluster is roll-up-classified (uniform DEMONSTRATED, evidenced below by spot-checks).

## The central framing (read first)

**The C3 fork ADRs are README-claim-driven and investigation-backed, not brokenness-assumed.** The dominant pattern: a README claim (e.g. "5 consensus algorithms", "8 worker types", "topology differentiation") is quoted at line-number, then validated against the runtime via source archaeology (`grep -r TopologyManager cli/`), orphan-detection (66 swarm records counted; 6 orphaned `swarm/src/` implementations), and **5-panellist investigation hives** (ADR-0115 git archaeology, ADR-0140 4-arm reproduction). This is the same evidence-discipline C2 found — the risk is **demonstrated-then-but-diverged-since** + **un-merged-upstream-fix**, not fabricated brokenness.

**The one real fork REGRESSION in C3 (`agent_execute` stale models) is NOT covered by any C3 ADR** — it is an *audit gap*: a paired upstream fix that the fork's upstream-sync audit took half of (the provider-routing half, `#2042`) and missed the other half (the model-catalogue half, `#1906`). See §The regression's provenance.

## ADR premise classification — divergence keystones + spine

| ADR | Surface touched | Problem statement | Class | Re-confirmed in C3 env? / mistake notes |
|---|---|---|---|---|
| **ADR-0098** (swarm-init sprawl) | `swarm_init`, claudemd-generator, swarm-tools | **66 orphan swarm records** counted in `.swarm/swarm-state.json` (all `running`, empty agents, 0 matching PIDs); timestamp analysis disproves per-project bootstrap; source-traced to generator "ALWAYS hierarchical" prose + handler blind-append (no dedupe). | **DEMONSTRATED (exemplary)** | Forensic on-disk count + source:line + adversarial-review options table (7 options, devil's-advocate rejections). **Re-confirmed in C3:** my env's CLAUDE.md carries the full anti-sprawl + ADR-0115 carve-out language; `swarm_init` minted exactly one record per distinct config (no orphan explosion). Fix is live. |
| **ADR-0114** (swarm vs hive architectural model) | swarm/hive conceptual boundary | The project had **no coherent shared model** for swarm vs hive; 4 parallel research agents returned 4 different framings; `ruflo-swarm` README mentioned "Hive-Mind Consensus" while declaring only `swarm_*` allowed-tools. | **DEMONSTRATED** | Multi-lens research (v2 ADRs, USERGUIDE, arch diagram, upstream source) + §Lens 10 establishing the 6-week 250-session working baseline. Defines the swarm=collaboration / hive=consensus split (`project-hive-mind-one-runtime-two-skills`). Foundational, not brokenness-repair. |
| **ADR-0115** (hive iterative-discussion regression) | claudemd-generator hive prose | **5-panellist investigation hive** found commit `32c13d322` (ADR-0098's sprawl fix) **side-swiped `hive-mind_spawn`** into the swarm-init anti-sprawl prohibition without separate justification; verified via `git log -S "hive-mind_spawn"` + the spawn mechanism reads CLAUDE.md (`hive-mind.ts:293 childSpawn('claude')`). | **DEMONSTRATED (exemplary)** | Git archaeology + verbatim diff + empirical "spawn reads CLAUDE.md" proof. This is the **ADR-0115 carve-out** the brief flagged as a hot area — it is a self-correction of ADR-0098's over-reach (a demonstrated regression *introduced by* a demonstrated fix — the C2 M2 scope-gap pattern). **Re-confirmed:** carve-out live in my CLAUDE.md ("NOT bundled into swarm-sprawl prohibition"). |
| **ADR-0105** (topology behavior differentiation) | `--topology`, hive-mind CLI | README promises "Swarm Topologies: 4 types" + per-topology behaviour, but `--topology` is **prompt-string interpolation** — `TopologyManager` (656 LOC, full runtime) exists in `swarm/src/` but `grep -r TopologyManager cli/` returns nothing (orphaned). | **DEMONSTRATED** | Source archaeology with file:LOC inventory + the wire-up-gap grep + candid v3 migration doc citation. Honest about the gap. |
| **ADR-0104** (hive-mind queen orchestration) | `hive-mind spawn --claude`, CLI defects | `hive-mind spawn --claude` **broken end-to-end**: Queen prompt forbade Task tool while pointing at a JSON-stub MCP path; **four named CLI defects** (greedy parser, silent default-substitution, false spawn-claim output, MCP attach fail in `-p`); no hive-state locking. | **DEMONSTRATED** | Four operationally-traced defects + the architectural mismatch. Honest that the `mcp__ruflo__hive-mind_*` tools were **"existing functional"** (distinguishes broken-CLI-wrapper from working-MCP-substrate — the exact counters-vs-content discipline the bar wants). |
| **ADR-0106** (consensus algorithm enforcement) | `hive-mind_consensus` | README quotes (8 specific line numbers) "5 consensus algorithms / BFT f<n/3 / 2-of-3 majority" — claims validated against runtime enforcement. | **DEMONSTRATED** | README-line-quoted premise. **Re-confirmed:** my `hive-mind_consensus propose` returned `strategy:bft, required:3, totalNodes:3` — real BFT quorum math. |
| **ADR-0108** (mixed-type worker spawns) | `hive-mind_spawn` | README "8 worker types" vs runtime spawn capability (line-quoted). | **DEMONSTRATED** | README-claim validation. **Re-confirmed:** `hive-mind_spawn agentType:researcher count:3` spawned 3 typed workers. |
| **ADR-0107** (queen-type differentiation) | queen types | whether strategic/tactical/adaptive select distinct runtime patterns. | **DEMONSTRATED → SUPERSEDED by ADR-0125** | Correctly evolved (T7 shipped prompt-prose + CLI validation; state-persistence deferred to 0125). Not stale — supersession is clean. |
| **ADR-0118 / 0118a / 0118b** (hive runtime-gaps tracker) | the 0119–0132 task series | Operationalised ADR-0103's 6 orphaned-implementation verticals into a 14-task (later 11+3) tracker; all marked `complete` 2026-05-03 with green-CI commits. | **DEMONSTRATED (tracker)** | Wave-based execution plan with per-task file:line + inter-wave gates. The per-task ADRs (0119 weighted / 0120 gossip / 0121 CRDT / 0122 memory-types-TTL / 0123 LRU-WAL / 0124 session-lifecycle / 0125 queen-type / 0126 worker-type / 0127 adaptive-topology / 0128 topology-runtime / 0131 worker-failure / 0132 sub-queen-escalation) each cite the orphaned `swarm/src/` impl they wire. **Re-confirmed:** hive memory-types+TTL, consensus strategies, queen/worker types all functional in my drives. |
| **ADR-0139 / 0140 / 0145** (hive-mind-advanced) | `hive-mind-advanced` skill | ADR-0103 catalogued 6 orphaned README-claim implementations; 0140 grounds the advanced skill in the **pre-regression working version** (ADR-0114 §Lens 10's 250+ council sessions). | **DEMONSTRATED** | Grounded in an empirically-working baseline + 4-arm reproduction (the sub-agent MCP-hang, ADR-0144). FORK-AHEAD protocol layer (`project-hive-mind-one-runtime-two-skills`). |
| **ADR-0144** (MCP tools from sub-agent context) | `hive-mind_memory` from sub-agents | 4-agent investigation + 5-persona council reproduced the `mcp__ruflo__hive-mind_memory` ~600s hang from Agent-tool sub-agent context. | **DEMONSTRATED → SUPERSEDED (folded into ADR-0140 Amendment)** | The premise (the hang) was reproduced 4 ways; the *transport-class rule* proposal was rescinded after reproduction narrowed it. Clean supersession. **Re-confirmed scope:** from my DIRECT MCP session `hive-mind_memory` does NOT hang (<5ms) — consistent with "sub-agent-context-only". |
| **ADR-0184 / 0185** (consensus handler port / CLI retirement) | `hive-mind_consensus` | port the fork-only `hive-mind-consensus-response.ts` handler; retire the duplicate CLI consensus path. | **DEMONSTRATED** | Handler-port + dedup. `project-hive-mind-one-runtime-two-skills` notes the fork-only consensus-response handler (ADR-0185). Re-confirmed: consensus tool functional. |
| **ADR-0254** (upstream ADR-129/130 disposition) | wasm `agent-wasm.ts`/`wasm-agent-tools.ts` + graph `graph_edges` + **rvagent WASM pin** | Council disposition: upstream ADR-129 (rvagent full integration, echo-stub→provider + 16 new tools) = **partial-pick**; ADR-130 (graph_edges schema) = **DEFER whole**. Fork on pre-129 echo-stub baseline but with a fork-only persistence layer + **newer rvagent pin (`0.2.1-patch.145`, "already exposes every method ADR-129 requires")**. | **DEMONSTRATED (exemplary) — and the re-pin premise is now LOAD-BEARING-CORRECT** | Two research traces + council + precise upstream-vs-fork posture at commit-SHA. **This ADR is why C3 wasm = 27-tool PARITY** AND why the C2 `graph_edges` regression exists (ADR-130 DEFER). **NEW (cross-env):** the prover proved upstream `wasm_agent_create` is **UPSTREAM-BROKEN** (`@ruvector/rvagent-wasm@0.1.0` glue-vs-binary skew). The fork's newer pin (now `0.2.1-patch.228`) **FIXES it** (my fork drive: `wasm_agent_create` succeeds ×3; the broken string has 0 occurrences in the fork WASM). So ADR-0254's "newer pin already exposes every method" was not just a demonstrated premise — it is the **fix for an upstream-broken feature**. FORK-AHEAD, not merge-tax. |
| **ADR-0256** (rvagent Phase 4 plugin-bridge) | `wasm_agent_compose includePlugins`, helpers | Phase-4 plugin-manifest reader port; **surfaced honestly** that the P4 helpers are unreachable without P2's `wasm_agent_compose` tool (corrects ADR-0254's "Phase 4 ships standalone"). | **DEMONSTRATED (exemplary)** | Verified helper line numbers against upstream `47a7825b0`; surfaced the P4↔P2 coupling the high-level disposition missed. Self-correcting. |
| **ADR-0266** (rvagent Phases 1-3 amendment) | the 17 new wasm MCP tools | Sequences the implementation of ADR-129 Phases 1-3 per the ADR-0258 (persistence-threading) + ADR-0259 (SAFE_MCP_TOOLS allowlist) design specs. | **DEMONSTRATED** | Implementation-sequencing ADR; the load-bearing decisions pre-decided in 0258/0259. **Re-confirmed:** all 27 wasm tools present + functional (gallery, create, prompt, introspection). The **ADR-095 G4 echo→Anthropic fallback** I found in `agent-wasm.js:89` is this lineage's documented mechanism. |
| **ADR-0202** (daemon RVF lifetime-lock break) | hook RVF lock path | daemon holds `.rvf.lock` for life → CLI/MCP hook callers get `LockHeld`. | **DEMONSTRATED (with corrected blast-radius)** | The lock contention is real for CLI/MCP/programmatic callers. **`project-two-hook-paths-cli-vs-handler` + `project-adr0202-hook-exit-code-source-truth` correct the framing:** the PRODUCTION settings.json path is file-based (`hook-handler.mjs`/`intelligence.cjs`, no RVF lock) so the user-facing severity was overstated; the audit's "EXIT:0" is a `_run_and_kill` harness artifact (source wires exit-1). **Not re-tested as a bug** per the brief — verified the two-path reality holds. |
| **ADR-0203** (eliminate parallel @claude-flow/hooks dead package) | hooks live vs dead path | The hook subsystem ships as **two parallel implementations**; the live path is `cli/src/mcp-tools/hooks-tools.ts` (4097 LOC), the dead path is `@claude-flow/hooks` (~11,850 LOC, never wired — `grep` proves zero imports + an ESM `require` bomb at `hooks/src/index.ts:233`). | **DEMONSTRATED (exemplary)** | **Triple dialectical re-review (5-of-5 at file:line)**; dead-ness confirmed on every axis; two mechanism claims self-corrected during review. Model of the discipline. **Re-confirmed:** the live `hooks_*` tools (route/model/worker/pre-task/post-task) all functional in my drives. |
| **ADR-0211** (init-emitted hook handler event completion) | settings.json hook handler | the init-generated hook handler's event completion. | **DEMONSTRATED** | (`project-adr0202` family). Production hook path confirmed file-based in my env (`hooks.json` → `ruflo-hook.sh`). |
| **ADR-0290** (automatic learning capture, Phase 1) | `hook-handler.mjs post-task` → episode → learner | upstream G6: `SubagentStop→post-task` hardcodes `intelligence.feedback(true)` (fabricated success); the learning layer was frozen (no trigger). | **DEMONSTRATED + FORK-AHEAD (fixes upstream G6)** | `project-adr0290-learning-capture-shipped` (shipped cli.415/agentdb.427, 17/17 smoke). **Re-confirmed in C3 env:** `hook-handler.mjs:192` DERIVES outcome from `tool_response.status`, `:197` skips on underivable (**no fabrication**), `:219` forwards the real `--success`; detached fire-and-forget episode path (`:213-233`) with escape hatches. The fork **removed upstream's G6 lie** + added the missing trigger. The optimizer-consumer gap (G1/G2) remains C1-owned. |

## The 7 plugin contract ADRs (ADR-0001 each)

| Plugin ADR | Premise | Class | Notes |
|---|---|---|---|
| **ruflo-core ADR-0001** | foundation contract: MCP server (300+ tools), 3 generalist agents, plugin catalog, smoke-as-contract. | **DEMONSTRATED** | Structural. **Re-confirmed:** 317 tools live, 3 agents (coder/researcher/reviewer), 12 workers, hooks routing all present. |
| **ruflo-swarm ADR-0001** | 12-tool MCP surface (4 swarm_* + 8 agent_*), anti-drift defaults, Monitor streaming. Sources cited at `swarm-tools.ts:71,145,208,270` + `agent-tools.ts:182,...`. | **DEMONSTRATED — but count drifted** | Counted-from-source. **BUT:** the contract pins **4** swarm_* tools; the fork live surface has **5** (added `swarm_scale`, FORK-AHEAD). The "4" is now an undercount of the fork's own surface (the README MCP-surface table also says 4). Minor staleness from the fork's own `swarm_scale` addition. |
| **ruflo-hive-mind ADR-0001 / ADR-0116/0118** | queen-led consensus surface materialising the USERGUIDE; "all USERGUIDE features have runtime support" (ADR-0118 §Status all `complete`). | **DEMONSTRATED** | The "known gaps vs USERGUIDE: all complete" claim is backed by the 0118 tracker. README honestly flags multi-round dialectic via broadcast as **aspirational/not-implemented** (ADR-0140) — exemplary honesty (it does not advertise the unbuilt path). |
| **ruflo-wasm ADR-0001** | 10-tool MCP surface (7 agent + 3 gallery), ADR-070 integration, sandbox isolation. | **DEMONSTRATED-at-authoring, NOW STALE** | The "10 tools" was true at authoring (pre-ADR-129). **Upstream + fork BOTH subsequently shipped ADR-129's 27-tool surface** (fork via ADR-0254/0256/0266). The README's "10 tools" is stale prose; the actual surface (27) is PARITY with upstream. Doc-drift, not a regression. |
| **ruflo-daa ADR-0001** | 8-tool MCP surface, namespace `daa-patterns`, intelligence-pipeline alignment, smoke-as-contract. | **DEMONSTRATED** | **Re-confirmed:** 8 daa_* tools functional; adaptation state mutation real. |
| **ruflo-workflows ADR-0001** | 10-tool MCP surface, lifecycle state machine, namespace `workflows-state`. | **DEMONSTRATED** | **Re-confirmed:** 10 workflow_* tools, full state machine (created→running→paused→running→cancelled). Note: `cancel` emits `failed` not the documented `cancelled` (M-C3 below). |
| **ruflo-goals ADR-0001** | GOAP/dossier workflow contract, 6 legacy namespaces with mapping, smoke-as-contract (10 structural checks). | **DEMONSTRATED + honestly scoped** | Honest that GOAP A* is **agent-level reasoning** (`goal-planner` agent) with **no dedicated MCP tools** — composes task_*/memory_*/workflow_*/hooks_intelligence/neural_predict. The 6 legacy namespaces (`adr`/`dossier`/`research`/`research-sources`/`horizons`/`horizon-sessions`) are documented non-compliant with migration deferred — recorded, not hidden. |

## The regression's provenance — `agent_execute` stale model catalogue (the audit gap)

This is the ONE C3 fork regression and it has a precise, documented provenance trail that makes it a textbook **un-merged-upstream-fix / paired-fix-half-taken** mistake:

| Step | Evidence |
|---|---|
| Upstream shipped the fix | `16e59c261` "fix(#1906): agent_execute model aliases → current Claude 4.x ids (not deprecated claude-3.x) (#1908)" — **2026-05-11**. Body: *"ids the Anthropic API now 404s. Every agent_execute call (and the WASM agent's LLM fallback, which routes through resolveAnthropicModel) failed with HTTP 404 even with a valid key."* |
| The fork took the PAIRED fix but not THIS one | INTEGRATION-LEDGER row 184: `94b98c90c`/`#2042` (agent_execute → v3 provider routing) **hand-ported** as `1c31b3ecc` under **ADR-0252** (2026-05-25 audit). The ledger row notes `resolveOpenAICompatModel()` was added — but the **Anthropic MODEL_MAP catalogue (the `#1906` fix) was NOT picked up.** `#1906` (2026-05-11) and `#2042` (2026-05-21) are sibling fixes to the same file; the audit took #2042, missed #1906. |
| The fork's own later touch sat on the stale state | `fd6d5c268` (ADR-0268 pre-task skill injection, **2026-05-29** — 18 days after the upstream fix) edited `agent-execute-core.ts` on top of the pre-#1906 MODEL_MAP and didn't update it. |
| Confirmed un-merged | `git merge-base --is-ancestor 16e59c261 HEAD` = **NO**. **No INTEGRATION-LEDGER row, no ADR** for the #1906 catalogue fix. Stale on released dist (`agent-execute-core.js:34-37`) AND fork HEAD (`.ts:57-60`). |

**This is NOT an assumed-brokenness premise** (no C3 ADR claims agent_execute works) — it is an **omission**: the fork's upstream-sync audit (ADR-0252) dispositioned the paired provider-routing fix and silently left the model-catalogue half behind. Disposition → C3 re-convergence ADR: cherry-pick/hand-port `16e59c261` (4 MODEL_MAP lines + `DEFAULT_ANTHROPIC_MODEL`) + add the missed INTEGRATION-LEDGER row; verify the wasm LLM fallback simultaneously (shared `resolveAnthropicModel`).

## Mistake catalogue (ADR-0291 §Confirmation taxonomy + C2 classes)

| # | Mistake type | Where | Severity | Detail |
|---|---|---|---|---|
| M-C1 | **Un-merged upstream fix / paired-fix-half-taken** *(C3's signature mistake)* | ADR-0252 audit (agent-execute) | **HIGH** | The fork took `#2042` (provider routing) and missed the paired `#1906` (model-catalogue 4.x ids). `agent_execute` is dead on every model tier; the wasm LLM fallback shares the bug. No ledger row. This is the inverse of C1's assumed-brokenness and distinct from C2's necessity-debt: it is a **completeness gap in an upstream sync**. FORK-REGRESSION #1. |
| M-C2 | **Scope-gap regression introduced by a demonstrated fix** | ADR-0098 → ADR-0115 | (already self-corrected) | ADR-0098's swarm-sprawl fix side-swiped `hive-mind_spawn` into the prohibition. **ADR-0115 already caught + fixed this** (the carve-out). Logged as a pattern instance (same class as C2's M2 ADR-0276 scope-gap), NOT an open item — the fork's own investigation hive closed it. |
| M-C3 | **Doc/contract staleness from surface evolution** | ruflo-wasm ADR-0001 (10 tools) + ruflo-swarm ADR-0001 (4 swarm_*) + workflow `cancel`→`failed` | LOW | (a) ruflo-wasm "10 tools" is pre-ADR-129; actual = 27 (PARITY w/ upstream). (b) ruflo-swarm contract pins 4 swarm_*; fork has 5 (own `swarm_scale` addition). (c) `workflow_cancel` emits status `failed` while the README state machine documents `cancelled`. All cosmetic; the surfaces work. |
| M-C4 | **Response-shape ergonomics (counters-vs-content adjacent)** | `wasm_agent_create`/`_prompt` triple-wrapping; wasm no-key NOTE possibly stripped (W-1) | LOW | wasm tools return a triple-nested MCP envelope (other 316 tools single-wrap); a single-parse consumer gets the inner envelope, not the payload. And the documented no-key `[NOTE: set ANTHROPIC_API_KEY...]` honesty suffix did not appear in my capture (open — prover to confirm). Honesty/ergonomics smell, not a brokenness. |
| M-C5 | **Un-ledgered fork-surface divergence (upstream tool dropped + replaced via different seam)** | ADR-0290 (vs upstream #2245/ADR-074); `hooks_task-completed`/`_teammate-idle`/`agent_logs` | LOW-MEDIUM | The fork did NOT merge upstream `aca2280f1` (#2245 self-learning wiring) → 3 upstream MCP tools absent. **ADR-0290 deliberately re-implemented `hooks_task-completed`'s capability via its own seam** (documented, FORK-AHEAD) — so this is NOT a capability regression. BUT: (a) **no INTEGRATION-LEDGER row** records the #2245 non-merge / tool-surface divergence (the ledger-discipline gap `feedback-update-integration-ledger` warns about), and (b) the `hooks_task-completed` MCP *name* is gone with no compat alias. Disposition: add the ledger row; decide alias-vs-accept. `_teammate-idle`/`agent_logs` removal is the ADR-0210 DELETE-the-synthetic-stub disposition (defensible). |

**Crucially — NO C3 fork ADR was built on a fabricated / un-probed brokenness premise.** Every audited premise was demonstrated of the fork's state at authoring time: orphan-record counts (0098), git archaeology + verbatim diffs (0115), source:LOC archaeology + wire-up greps (0105), four operationally-traced CLI defects (0104), README-line-quoted claims validated against runtime (0106/0108), triple dialectical file:line review (0203), upstream-SHA-verified disposition (0254/0256). Multiple ADRs **self-correct** their predecessors (0115 corrects 0098's over-reach; 0256 corrects 0254's "P4 standalone"; 0144 rescinds its own transport-class rule after reproduction; 0203 corrects two of its own second-pass mechanism claims). This is the same over-demonstrated posture C2 found.

**The C3 risk profile = C2's, plus one twist:**
- Like C2: **demonstrated-then-but-diverged-since** (the contract counts drifted as the surface grew — M-C3) and **scope-gap-from-a-good-fix** (M-C2, already closed by 0115).
- **Unlike C2 and C1:** the one live regression is a **completeness gap in an upstream sync** (M-C1) — a *paired* upstream fix where only one half was hand-ported. Not a wrong premise, not a necessity-debt — an omission the null hypothesis surfaced (upstream works; the fork fell behind on the catalogue).

## Roll-up classification — the hive-mind cluster (not individually deep-audited)

The ~26-ADR hive-mind cluster (0104–0132 + 0138–0145 + 0184/0185) is a **single coherent program**: ADR-0103 catalogued 6 README-claim implementations orphaned in `swarm/src/`; ADR-0114 set the swarm/hive model; ADR-0118 tracked the closure as 11+3 task ADRs (0119–0132), each wiring a specific orphaned runtime (weighted/gossip/CRDT consensus, memory-types+TTL, LRU-WAL, session-lifecycle, queen-type, worker-type, adaptive-topology, topology-runtime, worker-failure, sub-queen-escalation); ADR-0139/0140/0145 built the advanced council skill on the pre-regression baseline; ADR-0184/0185 ported the consensus handler + retired the duplicate CLI. **Spot-checks (0098, 0104, 0105, 0106, 0108, 0115, 0254, 0256, 0203) all classed DEMONSTRATED with on-disk/source/git/council provenance.** None exhibited the fabricated-brokenness pattern. A full per-ADR pass of the remaining ~17 hive runtime-gap ADRs is deferred to the C3 disposition phase if the queen wants exhaustive coverage; the divergence-relevant keystones + the sole regression's provenance are all audited above.

## Summary statistics

- **ADRs deep-audited: 23** (3 swarm/coord: 0098/0105/0114 + 8 hive keystones: 0104/0106/0107/0108/0115/0118/0140/0144 + 2 hive port: 0184/0185 + 3 wasm: 0254/0256/0266 + 4 hooks: 0202/0203/0211/0290 + 7 plugin contracts; overlapping count = 23 distinct rows above).
- **Problem statement DEMONSTRATED: 23 / 23** (100%). Every audited C3 fork ADR's premise was evidence-backed at authoring time (orphan counts, git archaeology, source:LOC, README-line quotes, investigation hives, dialectical file:line review, upstream-SHA disposition).
- **Two fork ADR premises are now LOAD-BEARING-CORRECT beyond demonstration** (the null hypothesis surfaced upstream weakness, not fork weakness): **ADR-0254** (rvagent re-pin) FIXES upstream-broken `wasm_agent_create`; **ADR-0290** (learning capture) FIXES upstream G6 fabrication. These are FORK-AHEAD wins where re-enabling the upstream mechanism would NOT have sufficed (upstream's mechanism is the broken `@0.1.0` WASM / the fabricated `feedback(true)`).
- **ASSUMED-brokenness premises: 0.** No C3 ADR fabricated or un-probed its problem statement.
- **DEMONSTRATED-then-but-diverged-since / staleness: M-C3** (3 instances, LOW) — contract counts (wasm 10→27, swarm 4→5) + workflow cancel-label.
- **The sole live FORK-REGRESSION is NOT an ADR premise failure** — it is **M-C1, an upstream-sync completeness gap** (paired fix half-taken): `agent_execute` stale models, ADR-0252 audit took `#2042` and missed `#1906`. HIGH severity, LOW fix-risk.
- **Mistake-class distribution:** fabricated-brokenness **0**; wrong-shape testing **0**; counters-vs-content **0** (the ADRs consistently distinguish working-substrate from broken-wrapper, e.g. ADR-0104's "MCP tools existing functional"); stale-checkout reads **0** (ADRs use `git show origin/main` / upstream SHAs); scope-gap-from-good-fix **1** (M-C2, *already self-closed* by ADR-0115); **un-merged-paired-upstream-fix 1** (M-C1, the new C3-signature class, OPEN — `agent_execute` models); **un-ledgered surface divergence 1** (M-C5, LOW-MEDIUM — the #2245 tool-drop is a deliberate ADR-0290 substitution but undocumented in the ledger + no compat alias). The C3 corpus is as clean on the ADR-0291 taxonomy as C2 — the one live FORK-REGRESSION (M-C1) and the surface-divergence (M-C5) both come from *upstream-sync handling* (an omission + an un-ledgered deliberate divergence), not from fabricated ADR premises. This is exactly what the null hypothesis is built to surface.

## Disposition inputs for the C3 re-convergence ADR (planning only — needs go-ahead)

| # | Item | Class | Disposition | Risk |
|---|---|---|---|---|
| 1 | `agent_execute` stale MODEL_MAP (M-C1, FORK-REGRESSION #1) | un-merged upstream fix | **Cherry-pick/hand-port `16e59c261`** (4 MODEL_MAP lines + `DEFAULT_ANTHROPIC_MODEL='claude-sonnet-4-6'`); add INTEGRATION-LEDGER row; verify the coupled wasm-prompt LLM fallback simultaneously (shared `resolveAnthropicModel`) | LOW (mechanical catalogue) |
| 2 | `hooks_task-completed`/`_teammate-idle`/`agent_logs` absent (M-C5) | deliberate fork substitution, un-ledgered | **Add the INTEGRATION-LEDGER row** for the #2245/ADR-074 non-merge + ADR-0290-substitution rationale; decide (a) thin `hooks_task-completed` alias→`trajectory-*` for upstream-surface compat OR (b) accept divergence + doc-note. `_teammate-idle`/`agent_logs` = keep removed (ADR-0210 synthetic-stub) after DA confirms no consumer | LOW |
| 3 | `swarm_scale` FORK-AHEAD (upstream doc-drift phantom) | fork wired the phantom | **KEEP-with-justification** (additive; the fork did upstream's unfinished work). Re-justify in `04-dispositions` | NIL |
| 4 | `wasm_agent_create` re-pin FORK-AHEAD (fixes upstream-broken) | fork fixed third-party WASM | **KEEP** (load-bearing fix). ADR-0254 already justifies the pin | NIL |
| 5 | `workflow_cancel`→`failed` not `cancelled` (M-C3) | doc/contract drift | trivial: emit `cancelled` per the README state machine OR update the README | NIL |
| 6 | ruflo-wasm README "10 tools" + ruflo-swarm "4 swarm_*" (M-C3) | stale contract counts | update READMEs/contracts to the real surface (27 wasm, 5 swarm) | NIL |
| 7 | wasm triple-wrapping (M-C4) + no-key NOTE (W-1) | response ergonomics | flatten the wasm MCP envelope to single-wrap; confirm/fix the NOTE suffix | LOW |
