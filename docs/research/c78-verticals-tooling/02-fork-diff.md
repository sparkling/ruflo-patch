# C7+C8 Domain verticals + Developer tooling — Fork Diff (classified deltas)

**Protocol:** ADR-0292 step 4. **Null hypothesis:** upstream works; fork divergence is the bug (FORK-REGRESSION is the default for any delta). **Validation bar:** ADR-0291 §Confirmation (5-point) — binding before any UPSTREAM-BROKEN verdict. **Method additions (C2/C3/C4/C5):** cold-vs-warm + drive in the *documented arg shape* (the live tool/CLI schema is the spec, not the skill prose) + the self-disclosure lens (honest degradation self-discloses via note/warn; real defects fail silently). **Vertical-specific safety:** NO external feeds / paid services / API keys / market data / Cognitum hardware; trading = no-key honest-gate verification only; numerical kernels driven on synthetic SPD/graph data; plugin-creator scaffolds confined to `/tmp`.
**Author:** fork-auditor (queen-led C7+C8 swarm, combined final categories). **Date:** 2026-06-04.

> **DA review (2026-06-04) — kernels/ledger/honesty-fix claims UPHELD; the headline REFUTED** (`/tmp/c78-evidence/da/logs/`). **"FORK-REGRESSION: 0" is FALSE:** live drives show fork `ruvllm_hnsw_create` → `mod.initSync is not a function` while upstream succeeds — and the KNOWN-table's "NOT a C7/C8 surface" exclusion was wrong: `market-ingest` declares `ruvllm_hnsw_create/add` and `market-pattern` declares `ruvllm_hnsw_route` — **ruvllm IS market-data's HNSW substrate**, so C7 carries ONE (known, tracked) FORK-REGRESSION = ADR-0293 D1, fix implemented-pending-release; market-data's advertised HNSW path is degraded until it ships. Also DA-corrected: (a) the transfer fabrication settlement — SHARED-inherited demo-FALLBACK (real IPFS+Ed25519 chain first; stderr `(demo)` disclosure; the MCP envelope drops the marker — fork honesty-patch candidate), confirmed artifact-parity (no C5-style drift); (b) `smoke-portfolio-cg` stays RED even with `skills/` materialized — its grep literal `mcp__claude-flow__memory_store` was never rebranded (the skill correctly uses `mcp__ruflo__`), and the 3 kernel smokes are NOT wired into any CICD runner (latent red); (c) a second split-surface drift both researchers missed: fork `commands/market.md:11` still prescribes `agentdb_hierarchical-store` + namespace (the exact bug its ADR-0001 §19 fixed in the SKILL — shared with upstream); (d) settled: `@noble/ed25519` is bundled + both degradation modes are LOUD; iot installs `@latest` (no version-skew — the "alpha.1" was the demo catalog's fabricated value); `mcp__ruflo-sublinear__*` unregistered both sides (fallback honest); embeddings dim note — fork mpnet-768 vs upstream MiniLM-384 (known by-design divergence, unreconciled in these docs).

> **Prover status — the C7/C8 `01-upstream-proof.md` had NOT landed when I drove.** Unlike C5 (where the prover's 01 landed mid-write), the c78 prover's deliverable was absent at write time (only `c6-operations/01-upstream-proof.md` existed as a sibling; the `c78-verticals-tooling/` dir did not yet exist — I created it). I therefore drove the fork WITHOUT a parallel upstream baseline and substituted **direct upstream-source comparison** (`git -C ruvnet/ruflo show origin/main:<path>`, ref **`367cb82ad`** ≈ 3.10.12) for every load-bearing classification. Where the prover's live 3.10.36 baseline would refine a verdict, I flag it as an open question. **Net: the C7/C8 surface is overwhelmingly markdown-thought-templates over MCP primitives + two external-package/external-CLI plugins; the substantive drivable code is neural-trader's fork-authored numerical kernels, which I drove end-to-end on synthetic data.**

## Scope note (binding — read first)

**The 4 C7/C8 plugins are heterogeneous, and 3 of the 4 are pure prose scaffolds** (the ADR-0248 §Cluster-D finding, re-confirmed): they compose central `mcp__ruflo__*` primitives via markdown skills — there is no plugin-side runtime to drive except the shared substrate. I read every plugin spec from the **committed fork tree** (authoring truth, ref **`c59d1b6c9`** — `fix(embeddings): ADR-0294 B1…`; the live working tree was NOT read, an implementer is actively landing ADR-0294) and **drove the substrate** the skills call:

- **ruflo-market-data** (C7) — prose scaffold; 2 skills + 1 agent + 5 CLI subcommands (`market ingest/patterns/search/history/compare`). Composes `memory_*` (namespaced), `embeddings_generate`, `agentdb_pattern-*`, `ruvllm_hnsw_*`. **The plugin does NOT ingest feeds** (the manifest self-discloses "data ingestion + normalization handled by user-supplied tooling, not the plugin"). Drove the substrate primitives + the namespace-routing fix premise.
- **ruflo-neural-trader** (C7) — the ONLY C7/C8 plugin shipping real fork-authored runtime source: `src/sublinear-adapter.{ts,mjs}` (CG kernel), `src/signed-attribution.{ts,mjs}` (single-entry PageRank + ed25519), `src/signed-artifact.{ts,mjs}` (Ed25519 backtest signing), `src/pipeline-messages.ts`. 9 skills wrap the **external `npx neural-trader` CLI** (NOT installed — honest-gate verification). Drove the 3 numerical/crypto kernels end-to-end on synthetic SPD matrices + synthetic attribution graphs + a throwaway ed25519 keypair, plus the 3 committed kernel smokes.
- **ruflo-iot-cognitum** (C7) — external-package-backed (`@sparkleideas/plugin-iot-cognitum`, the Cognitum Seed hardware bridge, `@cognitum-one/sdk/seed`); requires a physical Seed device at `http://169.254.42.1`. **Cannot be driven** (no hardware; safety rule = synthetic local only). Classified the SUBSTRATE (5 `memory_*` namespaces) + the marketplace honest-gate + the registry-catalog honesty, exactly as C5 did for federation.
- **ruflo-plugin-creator** (C8) — meta-scaffolder; 2 skills (`create-plugin`/`validate-plugin`) + 1 agent + 1 command. **This is where `transfer_plugin-*`/`transfer_store-*` intersect** (the brief's FORK-AHEAD candidates): `create-plugin/SKILL.md` declares `allowed-tools: mcp__ruflo__transfer_plugin-info mcp__ruflo__transfer_plugin-search mcp__ruflo__transfer_store-search` and uses `transfer_plugin-search` (step 2, "check name not taken"). Drove those transfer tools directly.

## Environment record (exact)

| Component | Value |
|---|---|
| Fork project | `/tmp/ruflo-fork-c78` — `ruflo init --full --force` + `ruflo memory init` ("Verification passed, router healthy, 19 controllers"), Verdaccio (`http://localhost:4873`), `npm_config_cache=/tmp/ruflo-fork-c78/.npm-cache` |
| `@sparkleideas/ruflo` (wrapper) | `3.1.0-alpha.14-patch.389` |
| `@sparkleideas/cli` | **`3.7.0-alpha.10-patch.415`** (matches expected) |
| `@sparkleideas/agentdb` | **`3.0.0-alpha.14-patch.427`** (matches expected) |
| `@sparkleideas/cli-core` | `3.7.0-alpha.5-patch.190` |
| `@sparkleideas/memory` | **`3.0.0-alpha.15-patch.415`** — neural-trader's ADR-125 dep; **README/ADR claim `@sparkleideas/memory@3.0.0-alpha.18`**, live is `alpha.15-patch.415` (a doc-vs-live version-pin drift — D-NT-2 below) |
| `neural-trader` (external CLI the trader skills wrap) | **NOT installed** (deliberate — honest-gate test; the skills self-disclose + `npm ls neural-trader \|\| npm install`) |
| `@noble/ed25519` (kernel crypto dep) | present in npx cache `_npx/906e6debb112be6d/node_modules` (resolves; signing roundtrip works) |
| Node | v24.14.1 (native better-sqlite3) |
| MCP serverInfo | `ruflo 3.0.0`; same registry as C2-C6 |
| C7/C8-relevant tool families | `transfer_plugin-*` (search/info/featured/official = 4), `transfer_store-*` (search/info/featured/trending/download = 5), `transfer_detect-pii` + `transfer_ipfs-resolve` (2); substrate: `memory_*`, `agentdb_pattern-store`, `agentdb_hierarchical-store`, `embeddings_generate`. No plugin in C7/C8 contributes its own central MCP tool (all compose the shared registry) |
| Memory backend (live) | `archivist (RVF + HNSW)` — `.swarm/memory.rvf` authoritative (the C2 J1 substrate posture) |
| Fork source ref (pinned) | committed **`c59d1b6c9`** — read-only via `git show`; the live fork working tree NOT read (implementer actively editing) |
| Upstream compare ref | `ruvnet/ruflo` `origin/main` **`367cb82ad`** (~3.10.12). **Caveat:** older than the prover's live 3.10.36 — byte-diffs cited from `git show origin/main`; behaviour at 3.10.36 deferred to the prover when it lands |
| Raw evidence | `/tmp/c78-evidence/fork/` — `drives/` (cg-parity-driver.mjs + extracted kernels), `logs/` (init, smoke-cg/attr/signing, cg-parity), `nt-pinned/` (pinned-ref kernel tree for smoke runs) |

> **Harness note (per C2/C5 lesson):** the `NODE_OPTIONS --require` fs-write-shim was **NOT used** (deadlocks the fork's native RVF write loop — C2 lesson; brief mandate). Persistence/behaviour verified by direct schema inspection + live MCP drives + cold-process re-runs + fork-vs-upstream `git show` byte-diff. **Self-inflicted wrong-shapes caught:** the 3 committed kernel smokes report "FAIL: N issue(s)" — traced to the smokes' `[2/3]`/`[3/3]` **static SKILL.md call-site checks** finding no SKILL.md (I materialized only `src/` + `scripts/` into the pinned tree, not `skills/`); **every runtime kernel/crypto/reproducibility assertion that printed shows ✓** (grep for `✗` in the logs = 0). No kernel classified on a wrong-shape failure. All spawned processes are short-lived `node` invocations (none left running); the long-running env-build completed (exit 0); live patch daemon untouched.

## Headline divergences

1. **C7/C8 is the cleanest, lowest-divergence category audited — like C5, it is upstream-MIRRORED with NO fork-originating regression.** 3 of 4 plugins are markdown scaffolds over the shared substrate; the 4th (neural-trader) ships real numerical code that is **upstream-authored (ADR-126 `#2068`) and hand-ported** (ADR-0257, ledger rows 169-173) — PARITY-via-handport, not fork-ahead. **Zero FORK-REGRESSION.**
2. **The neural-trader fork-authored kernels are sound and their headline claims hold under independent drive.** CG parity guarantee `||cg − neumann||_∞ < 1e-4` HOLDS (measured 1.6e-11 … 4.1e-11 at n=16/64/256); PageRank reproducibility (same seed → byte-identical scores + ordering) HOLDS; ed25519 sign/verify roundtrip + tamper-rejection + key-pinning HOLD. The skills self-disclose loudly ("NEVER silently fall back", `[WARN] … UNSIGNED degraded mode`, `attribution_method: "input-zscore-fallback"`) — the GOLD self-disclosure pattern, the inverse of C1's hooks_transfer fabrication.
3. **The `transfer_plugin-*`/`transfer_store-*` families (plugin-creator's FORK-AHEAD candidates) return a HARDCODED demo/seed catalog with fabricated metadata** (placeholder CIDs `bafybeiiotcognitumplugin2026`, `totalDownloads:50000`, `reputation:100`, placeholder `signature:"ed25519:genesis-pattern-signature"`/`publicKey:"ed25519:claude-flow-team-key"`, `lastUpdated:now`). **This fabrication is BYTE-IDENTICAL in upstream** (`discovery.ts:247/253-256/288-289/1055` at origin/main `367cb82ad`) → **SHARED/PARITY, NOT a fork regression** (the fork only codemod-rebrands the `@claude-flow/`→`@sparkleideas/` IDs). It is the same *class* as ADR-0293 D2 (hooks_transfer demo-data) but inherited, not fork-introduced. A name-search (`transfer_plugin-search`) honestly returns `{total:0}` — the one path plugin-creator actually uses is honest.
4. **The ADR-0248 F-07-006/F-07-007 honest-description rewrite landed in `plugins/*/.claude-plugin/plugin.json` but NOT in the top-level `.claude-plugin/marketplace.json`** — a split-surface honesty gap. The marketplace manifest still carries `"witness chain verification for Cognitum Seed hardware"` (iot) — **the exact string in the marketplace-integrity lint's own `OVERCLAIM_DESCRIPTION_PATTERNS`** (line 86) — because the lint's Assertion 4 walks only per-plugin manifests, never `marketplace.json`. **SHARED with upstream** (upstream marketplace.json byte-identical). Demonstrated, LOW severity, material to the honesty program. (D-CROSS-1.)
5. **The substrate namespace-routing bug-fix premise (the recurring `embeddings_embed`→`embeddings_generate` + namespace-to-`agentdb_*` → `memory_*` class that ALL 4 plugins' ADR-0001s + plugin-creator's drift table encode) is DEMONSTRATED at the live-schema level** — `agentdb_pattern-store` accepts only `{pattern,type,confidence}` (no namespace field); `agentdb_hierarchical-store` accepts only `{key,value,tier}` (enum working/episodic/semantic, no namespace); `embeddings_embed` is absent / `embeddings_generate` present; `memory_store` DOES carry a `namespace` param. The fixes are the exact, correct remedy. PARITY (these are shared-with-upstream fixes, the same class C2 already characterized).

## Classified delta table

Legend: **FORK-REGRESSION** (works upstream, broken/diverged in fork — the default) · **FORK-AHEAD** (fork capability absent upstream) · **PARITY** · **PARITY-via-handport** (fork hand-ported upstream's feature; sub-case of PARITY) · **UPSTREAM-BROKEN** (full bar + named root cause) · **DOC-DRIFT** (advertised prose ≠ live tool/CLI; flagged, not a behavioural break) · **SHARED** (same latent issue both sides) · **NOT-DRIVABLE** (out of safety scope; classified on substrate + source).

### ruflo-neural-trader — fork-authored numerical/crypto kernels (the one substantive C7/C8 runtime surface)

Driven end-to-end on synthetic SPD matrices (CG), synthetic attribution graphs (PageRank), and a throwaway ed25519 keypair (signing). Evidence: `logs/cg-parity.log`, `logs/smoke-{portfolio-cg,feature-attribution,backtest-signing}.log`, `drives/cg-parity-driver.mjs`.

| Feature | Upstream (source `367cb82ad` + ADR-126 `#2068` provenance) | Fork (pinned `c59d1b6c9`) | Class | Evidence |
|---|---|---|---|---|
| `conjugateGradient` (CG kernel, `sublinear-adapter.mjs`) | upstream-authored ADR-126 Phase 3 / ADR-123 Wedge 8; hand-ported (ledger row 171, `9c075a3c3`→`8464156a1`) | CG solves SPD to analytical answer (within 1e-6); converges in ≤n iters (12/15/17 at n=16/64/256, SPD theory) | **PARITY-via-handport** — sound, correct | cg-parity-driver 15/15; smoke-cg `[3/3]` all ✓ |
| CG parity guarantee `‖cg − neumann‖_∞ < 1e-4` (README headline) | upstream design claim | **HOLDS decisively** — measured **1.6e-11 / 3.0e-11 / 4.1e-11** at n=16/64/256 (>7 orders of margin) | **PARITY-via-handport** (claim TRUE) | cg-parity.log |
| CG vs Neumann speedup magnitude ("40-60×, ~816ns vs ~50µs at n=256") | upstream **native-WASM** `sublinear-time-solver@1.7.0` bench (via `mcp__ruflo-sublinear__solve`) — explicitly NOT the JS fallback | JS-fallback CG vs JS Neumann = **2-4× wall + 1.9-2.7× iter-ratio** (the local path); native 40-60× path requires the `ruflo-sublinear` plugin (NOT registered) | **PARITY-via-handport + DOC honesty-OK** — the 40-60× is correctly scoped to the native path; fallback self-discloses `path:'cg-local'`. NOT falsified (unmeasurable on fallback), honestly attributed | cg-parity.log; README §"Portfolio CG path" |
| CG degraded-flag honesty (non-SPD) | upstream design | non-symmetric → `degraded:true, reason:"matrix not symmetric within 1e-9"`; non-square → `degraded:true, reason:"row 0 is not length 2"`; valid SPD → `path:cg-local`, not degraded | **PARITY-via-handport** (honest self-disclosure) | cg-parity-driver |
| `localSingleEntryPageRank` reproducibility (`signed-attribution.mjs`) | upstream ADR-126 Phase 6; hand-ported (row 173, `11c1ad974`→`136d82183`) | **same seed → byte-identical scores AND identical top-K ordering** across runs (warm + cold process); different seed → different scores (seed load-bearing); mass conservation (Σ≈1); top-K excludes source, sorted desc, 1-indexed | **PARITY-via-handport** (claim TRUE) | smoke-attr `[3/3]` all ✓; cold-process re-run = true |
| `signAttributionArtifact`/`verifyAttributionArtifact` (ed25519) | upstream Phase 6 + CWE-347/#1922 key-pinning | roundtrip verifies; **tampered feature-score / tampered graphMetadata.seed / empty-sig / empty-key / wrong-key all FAIL**; verifier pins to caller `trustedPublicKey`, ignores served `witnessPublicKey` (pin is real, not a no-op) | **PARITY-via-handport** (sound; key-pin honest) | smoke-attr `[1/3]`+`[2/3]` all ✓ |
| `signBacktestArtifact`/`verifyBacktestArtifact` (ed25519, `signed-artifact.mjs`) | upstream Phase 4; hand-ported (row 172, `48cb0a7ee`→`4f686f68d`) | roundtrip verifies; tampered body (sharpe inflated) FAILS; same key-pin discipline | **PARITY-via-handport** | smoke-signing `[1/3]`+`[2/3]` all ✓ |
| `.ts`/`.mjs` mirror-sync contract | upstream design (the .ts = type-shape source of truth; .mjs = runtime) | `.mjs` exports the runtime functions; `.ts` adds TS-only `interface` shapes; smokes' "TS exports X + MJS exports X" all ✓ | **PARITY-via-handport** (mirror holds) | smoke `[1/3]` all ✓ |
| 9 skills wrap external `npx neural-trader` (honest-gate) | external CLI; not installed | skills self-disclose: `npm ls neural-trader \|\| npm install`; **"NEVER silently fall back"**; `[WARN] … UNSIGNED degraded mode`; `--explain` absent → `attribution_method:"input-zscore-fallback"` tag | **PARITY + GOLD self-disclosure** — honest degradation; no fabrication | trader-backtest/trader-explain SKILL.md grep |
| `@sparkleideas/memory` lifecycle pin (ADR-125) | README claims `@sparkleideas/memory@3.0.0-alpha.18` (warm-HNSW/RRF+MMR/TTL/dedup) | live env resolves `@sparkleideas/memory@3.0.0-alpha.15-patch.415` | **DOC-DRIFT (doc-vs-live pin)** — D-NT-2; the prose pins a higher alpha than the env ships (lifecycle features may differ); LOW | env version probe |

### ruflo-market-data — prose scaffold over substrate (5 CLI subcommands + 2 skills)

Driven via the shared substrate (the plugin has no own runtime). Evidence: live `agentdb_pattern-store`/`agentdb_hierarchical-store`/`memory_store` schemas + smoke source.

| Feature | Upstream (source `367cb82ad`) | Fork (pinned `c59d1b6c9`) | Class | Evidence |
|---|---|---|---|---|
| ADR-0001 fix: `embeddings_embed` → `embeddings_generate` | same tool-name reality | `embeddings_embed` absent from registry; `embeddings_generate` present (1 of 7 embeddings_*) | **PARITY (shared correct fix)** — DEMONSTRATED at schema | live catalog |
| ADR-0001 fix: namespaced reads/writes via `memory_*` not `agentdb_hierarchical-*`/`agentdb_pattern-*` | upstream skills had the same namespace-arg bug (pre-fix) | `agentdb_pattern-store` schema = `{pattern,type,confidence}` (NO namespace); `agentdb_hierarchical-store` = `{key,value,tier}` (NO namespace); `memory_store` HAS namespace. The fix is the correct remedy | **PARITY (shared correct fix)** — DEMONSTRATED at schema; empirically `pattern-store` ignored bogus type/ran via archivist | live schemas; pattern-store probe `success:true` |
| `market-data` / `market-patterns` namespaces (substrate) | same namespaces | both kebab-case `<stem>-<intent>` compliant; routed via `memory_*` | **PARITY** | README + ADR-0001 |
| 5 CLI subcommands (`market ingest/patterns/search/history/compare`) | prose scaffold | documented; route to substrate (no plugin runtime to fail) | **PARITY (prose scaffold)** | README; smoke step 1 |
| smoke check 1 (`expected 0.2.0`) | (upstream smoke same family) | **FAIL** — plugin at `0.2.24`, smoke hardcodes `0.2.0` | **DOC-DRIFT (stale-smoke)** — D-CROSS-2 | smoke.sh:1 |
| README `@claude-flow/cli v3.6` pin | upstream same | present (smoke greps the literal → passes) but live cli is `3.7.0-alpha.10` | **DOC-DRIFT (stale-pin, SHARED)** | README; smoke step 8 |

### ruflo-iot-cognitum — Cognitum Seed hardware bridge (NOT-DRIVABLE; substrate + honesty classified)

NOT driven (no Seed hardware; safety rule). Classified the substrate (5 `memory_*` namespaces) + the marketplace/registry honesty. Evidence: source + live `transfer_plugin-official`.

| Feature | Upstream (source `367cb82ad`) | Fork (pinned `c59d1b6c9`) | Class | Evidence |
|---|---|---|---|---|
| Cognitum Seed device ops (register/fleet/firmware/witness/anomalies — 25 subcmds, 6 workers, `@cognitum-one/sdk/seed`) | external-package design (`@sparkleideas/plugin-iot-cognitum`, hardware-bound) | **NOT-DRIVABLE** — requires a physical Seed at `http://169.254.42.1`; backed by a separate npm package (like federation in C5) | **NOT-DRIVABLE (out of safety scope)** — SYMMETRIC with the prover (neither side can drive without hardware) | README "Hardware"; ADR-0079a |
| 5 `iot-*` namespaces (substrate: `iot-devices/-telemetry/-telemetry-anomalies/-anomalies/-audit`) | same namespaces | kebab-case compliant; routed via `memory_*` (substrate-durable, RVF) | **PARITY (substrate)** | ADR-0001 namespace audit |
| `transfer_plugin-official` entry for iot (registry self-image) | upstream catalog byte-identical (`@claude-flow/plugin-iot-cognitum`, `cid:bafybeiiotcognitumplugin2026`, `totalDownloads:50000`, `securityAudit.passed:true`) | live returns `@sparkleideas/plugin-iot-cognitum`, same fabricated CID/downloads/audit, `lastUpdated:now` | **SHARED (upstream-inherited demo catalog; ID rebranded)** — see D-TRANSFER-1 | transfer_plugin-official drive; upstream discovery.ts diff |
| 5-tier trust model (UNKNOWN→FLEET_TRUSTED) + Z-score anomaly + firmware state machine | hardware-bound; documented | documented in README; runtime lives in the external package (unreachable) | **NOT-DRIVABLE (documented, unverified)** | README |
| smoke step 5 trust-tier assertion | (upstream smoke same) | asserts the FEDERATION tier names (`UNTRUSTED/VERIFIED/ATTESTED/TRUSTED/PRIVILEGED`), NOT the iot model's own (`UNKNOWN/…/FLEET_TRUSTED`); passes because the federation tiers appear in the cross-ref paragraph | **DOC-DRIFT (smoke checks wrong tier names)** — D-IOT-1, LOW | smoke.sh:step5 |
| smoke check 1 (`0.2.0`) | same family | **FAIL** — plugin at `0.2.24` | **DOC-DRIFT (stale-smoke)** — D-CROSS-2 | smoke.sh:1 |

### ruflo-plugin-creator — meta-scaffolder + the `transfer_*` intersection (C8)

Driven the transfer tools directly (the surface the brief flagged). Evidence: live `transfer_plugin-search`/`-official`/`store-trending`; skill source.

| Feature | Upstream (source `367cb82ad`) | Fork (pinned `c59d1b6c9`) | Class | Evidence |
|---|---|---|---|---|
| `create-plugin` name-conflict check via `transfer_plugin-search` | upstream tool same | `transfer_plugin-search("neural-trader")` → `{plugins:[], total:0, hasMore:false}` — **honest empty** (name not taken ⇒ empty result; the one path the skill uses is honest) | **PARITY (honest)** | transfer_plugin-search drive |
| `transfer_plugin-official` / `transfer_store-trending` / `-featured` catalog (declared in `create-plugin`/`validate-plugin` allowed-tools) | upstream `discovery.ts` byte-identical fabricated catalog (placeholder CIDs/checksums/downloads/signatures, `lastUpdated:now`) | live returns the same fabricated catalog, IDs codemod-rebranded `@sparkleideas/*`; `seraphine-genesis` pattern with placeholder `signature:"ed25519:genesis-pattern-signature"` | **SHARED (upstream-inherited demo/seed fabrication; same class as ADR-0293 D2 but INHERITED, not fork-introduced)** — D-TRANSFER-1 | transfer_plugin-official + store-trending drives; upstream discovery.ts diff `367cb82ad` |
| MCP-drift warning table (the plugin's headline value: `embeddings_embed`/`agentdb_*`-namespace/`pattern`-vs-`patterns`/`19 controllers`) | upstream prose; the warnings encode the real tool-schema reality | **all 4 warnings are factually correct** vs the live schemas (verified: embeddings_embed absent, pattern-store/hierarchical-store have no namespace param, memory_* does) | **PARITY (DEMONSTRATED-accurate)** — the scaffolder warns about real bugs | create-plugin SKILL.md vs live schemas |
| `validate-plugin` checks 3-5 ("each skill in `plugin.json.skills`") vs `create-plugin` step 4 ("do NOT include skills/commands/agents arrays") | upstream same internal tension | **internal inconsistency** — validate-plugin checks arrays the scaffolder is told NOT to emit (Claude Code auto-discovers from dirs) | **DOC-DRIFT (fork-internal, SHARED)** — D-CREATOR-1, LOW; validate-plugin would no-op those 3 checks on a correctly-scaffolded plugin | validate-plugin vs create-plugin SKILL.md |
| ADR-0001 fix: drop "19 AgentDB controllers" myth | upstream had the myth | smoke step 5 regression-checks its absence; replaced with "defer to `agentdb_controllers` runtime (~15 MCP tools, 29 controllers)" | **PARITY (shared correct fix)** | smoke.sh:5; README drift table |
| smoke check 1 (`0.2.0`) | same family | **FAIL** — plugin at `0.2.28` | **DOC-DRIFT (stale-smoke)** — D-CROSS-2 | smoke.sh:1 |

### Cross-plugin: the marketplace-manifest honesty split (D-CROSS-1)

| Surface | Upstream (`367cb82ad`) | Fork (`c59d1b6c9`) | Class | Evidence |
|---|---|---|---|---|
| `plugins/*/.claude-plugin/plugin.json` descriptions (per-plugin manifest — what `claude plugin install` copies) | over-claiming originals | **HONEST rewrites landed** (iot "workflow scaffolds composing memory_* … not hardware-bound enforcement"; neural-trader "does NOT expose neural-trader's CLI tools as MCP-callable"; market-data drops "ingestion") — ADR-0248 F-07-006/007 | **FORK-AHEAD (honesty fix) over upstream** — fork closes upstream's overclaim at the per-plugin layer | dumped plugin.json; ADR-0248/0251 |
| `.claude-plugin/marketplace.json` descriptions (the install-DECISION/browse surface) | byte-identical over-claims (iot "witness chain verification for Cognitum Seed hardware"; market-data "ingestion"; neural-trader README header) | **STILL over-claiming** — marketplace.json carries the EXACT lint-forbidden phrase `"witness chain verification for Cognitum Seed hardware"`; the rewrite never touched this file | **SHARED over-claim + LINT-SCOPE GAP** — D-CROSS-1; the marketplace-integrity lint Assertion 4 walks ONLY per-plugin manifests (`walkPluginManifests()`), never `marketplace.json` → the forbidden phrase lives unlinted at the very surface the lint's message says it protects | marketplace.json diff (fork==upstream); `plugin-marketplace-integrity.test.mjs:86,110,235-263` |

## FORK-AHEAD inventory

**Two, both narrow and both honesty/discipline-shaped (NOT capability the upstream mechanism lacked):**

1. **Per-plugin `plugin.json` honest-description rewrites (ADR-0248 F-07-006/007, ADR-0251 close).** The fork rewrote the 4 plugins' (+ federation/kg) per-plugin manifests to honest "workflow-scaffold composing memory_*" framing, closing an upstream overclaim. **"Would re-enabling/porting upstream have sufficed?" — NO; upstream carries the overclaim verbatim, so this is a genuine fork-ahead honesty divergence.** KEEP-with-justification (recorded in ADR-0248/0251; DA dissent on neural-trader DELETE closed affirmatively by ADR-0251 after re-examination). **Caveat: the rewrite is incomplete — it skipped `marketplace.json` (D-CROSS-1).**
2. **The marketplace-integrity lint itself** (`tests/pipeline/plugin-marketplace-integrity.test.mjs`, ADR-0248 Option A) — a fork-only build-time gate (tool-ref resolution, surface-non-empty, hook-brand, overclaim-string). Upstream has no such gate. KEEP. **Caveat: Assertion 4's scope misses `marketplace.json` (D-CROSS-1) — the lint should be extended to walk the top-level manifest too.**

**The neural-trader kernels are NOT fork-ahead** — they are upstream ADR-126 `#2068` work hand-ported (ledger rows 169-173). The fork did exactly what the null hypothesis prescribes: it ported upstream rather than inventing. PARITY-via-handport.

## Known-RESOLVED / KNOWN items — verified current, NOT re-reported as fresh

| Item | Source | Current state in C7/C8 env |
|---|---|---|
| ruvllm WASM initSync skew (6 tools) | ADR-0293 D1 | ~~NOT a C7/C8 surface~~ **DA correction: it IS a C7 surface** — market-data's `market-ingest` (ruvllm_hnsw_create/add) + `market-pattern` (ruvllm_hnsw_route) depend on it; fork drive fails, upstream succeeds → C7's one (known, tracked) FORK-REGRESSION; D1 fix implemented-pending-release is the prerequisite for market-data's HNSW path |
| `hooks_transfer` demo-data fabrication | ADR-0293 D2 | **The analogous risk IS present in C7/C8** (the `transfer_plugin-*`/`transfer_store-*` fabricated catalog — D-TRANSFER-1) but is **SHARED/upstream-inherited** (byte-identical at origin/main `367cb82ad`), NOT the fork-introduced D2 class. The `transfer_plugin-search` name-lookup path (what plugin-creator uses) is honest. Recorded, cross-referenced, NOT re-attributed to the fork |
| neural hash-fallback (`neural_predict/status`) | ADR-0293 D3 | NOT a C7/C8 surface; cross-ref only |
| graph_edges starvation (ADR-0276 scope-gap) + rabitq | ADR-0294 R1/R3 | NOT a C7/C8 surface; cross-ref only (rabitq is a ruflo-agentdb surface, F-07-002 — already removed per ADR-0248) |
| transfer_plugin-*/transfer_store-* are FORK-AHEAD candidates | brief | **CLASSIFIED: NOT fork-ahead — SHARED with upstream** (the families + their seed catalog are upstream-authored; fork only rebrands IDs). The plugin-creator intersection is real (`create-plugin`/`validate-plugin` allowed-tools) but the one path used is the honest `transfer_plugin-search` name-lookup |
| neural-trader once a C5 grep hit | brief | confirmed unrelated to C5 (it surfaced as a marketplace `transfer_plugin-official` entry, not a security surface); fully owned here in C7 |
| plugin marketplace = canonical skill delivery | `project-plugin-system-is-canonical-skill-delivery` | **CONFIRMED** — all 4 ship via `marketplace.json` git-relative `source:./plugins/…`, NOT npm; drove the substrate + transfer tools, not the init-copied skills |
| RVF-as-sole-truth (memory.db = relational sidecars) | C2 J1 | **CONFIRMED** — the C7/C8 namespaces persist to `.swarm/memory.rvf` (substrate) |
| ADR-0248 F-07-001 graph-intelligence DELETE precedent | ADR-0248 | **Confirmed live** — `transfer_plugin-official` no longer lists graph-intelligence; the DELETE landed (ledger row 215). Sets the "delete a DOA fork-only plugin" precedent the neural-trader DA dissent invoked (closed by ADR-0251) |

## Drive coverage + gaps

**Driven (production/synthetic shape, content-verified):**
- **neural-trader kernels** (the substantive surface): CG correctness + parity-guarantee + speedup-direction + degraded-honesty on synthetic SPD (n=16/64/256, 15/15 independent asserts); PageRank reproducibility + mass-conservation + top-K (cold + warm process); ed25519 sign/verify roundtrip + 6-way tamper/key-pin rejection (attribution + backtest); `.ts`/`.mjs` mirror exports; 3 committed kernel smokes (runtime halves all ✓). Honest-gate self-disclosure in trader-backtest/trader-explain SKILL.md.
- **transfer surface** (plugin-creator intersection): `transfer_plugin-search` (honest empty for name-lookup), `transfer_plugin-official` (fabricated catalog — characterized + upstream-diffed), `transfer_store-trending` (placeholder-signature pattern — characterized). `agentdb_pattern-store` bogus-namespace probe (ignored, routes via archivist).
- **substrate bug-fix premise** (all 4 plugins): live schemas of `agentdb_pattern-store`/`agentdb_hierarchical-store`/`memory_store`/`embeddings_generate` — the namespace-routing fix DEMONSTRATED structurally.
- **marketplace honesty**: `plugin.json` (honest) vs `marketplace.json` (overclaim) split — grep-exact + lint-scope read + upstream byte-diff.

**Could NOT drive (recorded judgment calls — NOT failures):**
- **iot-cognitum hardware surface** (25 subcommands, 6 workers, Seed device ops) — requires a physical Cognitum Seed at `169.254.42.1` + `@cognitum-one/sdk`; **out of safety scope** (no hardware; synthetic-local only). SYMMETRIC with any prover (the package is hardware-bound). Classified on substrate + source + the marketplace honest-gate. Do NOT attempt.
- **neural-trader live trading / backtest / signal / regime / portfolio via `npx neural-trader`** — the external CLI is not installed (no-key honest-gate rule: I verified the skills self-disclose its absence + handle it, but did not install/run a trading engine). The kernels (the fork's own code) ARE driven; the external CLI delegation is honest-gate-only by mandate.
- **`mcp__ruflo-sublinear__solve` / `page-rank-entry` native-WASM path** — the `ruflo-sublinear` plugin is NOT registered in this runtime; the kernels' native path (the 40-60× claim) is unreachable. The fallback path (which I drove) self-discloses `path:'cg-local'`/`local`. The 40-60× magnitude is honestly attributed to the upstream native bench, not the fallback — recorded as a scoped-claim, not a defect.
- **`security defend` text-crash (C5 R2)** and **federation package-load (C5 R1)** — NOT C7/C8 surfaces; the C5 02-fork-diff owns them.

## Divergence counts

- ~~FORK-REGRESSION: 0~~ **DA-corrected: FORK-REGRESSION: 1 (KNOWN/tracked)** — `ruvllm_hnsw_*` dead on the fork degrades market-data's advertised HNSW substrate (= ADR-0293 D1, implemented-pending-release; the KNOWN-table exclusion was wrong). Otherwise the lowest-regression category audited.
- **FORK-AHEAD: 2** — both honesty/discipline-shaped, KEEP-with-justification: (1) per-plugin `plugin.json` honest-description rewrites [ADR-0248/0251; genuine fork-ahead, upstream carries the overclaim] — caveat: incomplete (skips marketplace.json, D-CROSS-1); (2) the marketplace-integrity lint [fork-only build gate] — caveat: Assertion 4 scope misses marketplace.json.
- **PARITY / PARITY-via-handport: ~16** — neural-trader kernels (CG correctness + parity-guarantee + PageRank reproducibility + 2× ed25519 signing + mirror-sync = ~8, all hand-ported ADR-126); the substrate namespace-routing fixes (pattern-store/hierarchical-store/embeddings_generate = 3); plugin-creator's MCP-drift warnings (accurate); the iot/market namespaces (substrate-durable); `transfer_plugin-search` honest name-lookup; the "19 controllers" myth-drop.
- **UPSTREAM-BROKEN: 0** (consistent with the null hypothesis). No full-bar candidate. (The transfer-catalog fabrication is SHARED-honesty-debt, not a named runtime breakage; it self-images rather than crashes.)
- **DOC-DRIFT (flagged): 6** — D-CROSS-1 marketplace.json overclaim + lint-scope gap [LOW, SHARED, material to honesty program]; D-CROSS-2 all-4 stale-smoke `0.2.0` literal [LOW, fork-internal]; D-TRANSFER-1 fabricated transfer catalog [LOW, SHARED, = ADR-0293-D2-class but inherited]; D-NT-2 neural-trader `@sparkleideas/memory` doc-pin alpha.18 vs live alpha.15 [LOW]; D-IOT-1 iot smoke checks federation tier-names not its own [LOW]; D-CREATOR-1 validate-plugin checks arrays create-plugin is told to omit [LOW, SHARED]. **5 of 6 are SHARED-with-upstream or fork-internal-cosmetic** — the C5/C2 pattern (the fork mirrors upstream incl. its drifts), NOT the C1 wrong-shape class.
- **NOT-DRIVABLE (out of safety scope): 1 plugin** — iot-cognitum hardware surface (SYMMETRIC; no hardware). Classified on substrate + honesty.
- **SHARED-with-upstream: 3** — D-CROSS-1 (marketplace overclaim), D-TRANSFER-1 (fabricated catalog), D-CREATOR-1 (validate/create tension) — all byte-confirmed identical at upstream `367cb82ad`.
- **KNOWN cross-ref (not re-investigated): ADR-0293 D1-D4, ADR-0294 R1/R3** — none are C7/C8 surfaces; the D2-analogue (transfer fabrication) was driven and is SHARED-inherited, NOT the fork-D2 class.

## Open questions routed to the prover / devil's advocate

1. **D-TRANSFER-1 — transfer-catalog fabrication at live 3.10.36.** I confirmed the seed catalog is byte-identical in upstream source `367cb82ad` (≈3.10.12). Does the prover's live **3.10.36** `transfer_plugin-official` / `transfer_store-trending` return the same fabricated demo data (placeholder CIDs, `totalDownloads:50000`, placeholder signatures)? If yes → SHARED confirmed at 3.10.36. If upstream-3.10.36 wired a real registry → reclassify the fork as stale-fork-behind. (Low stakes — the one plugin-creator path used is the honest name-search.)
2. **D-CROSS-1 — marketplace.json honesty at 3.10.36.** Upstream source `367cb82ad` marketplace.json carries the byte-identical iot overclaim. Does live 3.10.36 still ship `"witness chain verification for Cognitum Seed hardware"` in marketplace.json? (Confirms SHARED at 3.10.36 vs an upstream marketplace.json fix the fork missed.) **Disposition recommendation regardless:** extend the marketplace-integrity lint Assertion 4 to ALSO walk `.claude-plugin/marketplace.json` descriptions, and apply the F-07-006/007 rewrite there (the lint's own message says marketplace.json is the install-decision surface — it should look there).
3. **neural-trader native-WASM speedup arm.** The 40-60× CG / native-PageRank claim is attributable only to `mcp__ruflo-sublinear__solve` (the `ruflo-sublinear` plugin, not registered). If a prover can register it, re-run the bench to confirm the native magnitude (the fallback 2-4× is what I measured, honestly scoped). Low stakes — the fallback is correct + self-disclosing.
4. **iot-cognitum data-plane (Cognitum Seed) — SYMMETRIC coverage gap.** The hardware surface (device register/fleet/firmware/witness over a real Seed) is undriven by safety rule. A verdict needs a physical Seed + `@cognitum-one/sdk` (out of scope). Do NOT attempt. The substrate (5 `memory_*` namespaces) is PARITY.
5. **DA re-run recommendation:** the neural-trader kernels are the only C7/C8 surface with falsifiable numerical claims — recommend the DA independently re-run `drives/cg-parity-driver.mjs` + the 3 committed smokes (with the `skills/` tree materialized so the `[2/3]`/`[3/3]` SKILL.md halves also exercise) to cross-confirm the parity-guarantee + reproducibility + key-pin findings.
