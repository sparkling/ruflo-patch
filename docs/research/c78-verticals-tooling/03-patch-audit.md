# C7+C8 Domain verticals + Developer tooling — Patch-History Audit

**Protocol:** ADR-0292 step 5. For each fork ADR/patch touching the 4 C7/C8 plugins' surfaces, classify the **problem statement** as DEMONSTRATED (evidence of the failure in the ADR / linked artifacts / re-confirmed in the C7/C8 fork env) vs ASSUMED (asserted brokenness without production-shape proof). Plus the mistake catalogue (ADR-0291 §Confirmation taxonomy + C2's *demonstrated-then-but-diverged-since* class + C4/C5's *doc-drift* class).
**Author:** fork-auditor (queen-led C7+C8 swarm, combined final categories). **Date:** 2026-06-04.

> **DA review (2026-06-04) — 11/11 premises UPHELD; ledger rows 169-173/215/218 verified verbatim; the "(a) ZERO fork-originating regression" summary clause is corrected by the DA** (see 02's DA note): C7 carries ONE known/tracked FORK-REGRESSION — `ruvllm_hnsw_*` (ADR-0293 D1) is market-data's HNSW substrate, wrongly excluded as "not a C7/C8 surface". Minor cites: the marketplace-integrity lint pattern sits at line 91 (not 86) and `walkPluginManifests` at 114 — substance correct. New DA items for dispositions: smoke-portfolio-cg un-rebranded grep literal + the 3 kernel smokes unwired from CICD; the market-data command.md split-surface drift (sibling of D-CROSS-1).

> **Prover status:** the c78 `01-upstream-proof.md` had NOT landed at write time (see `02-fork-diff` §Prover status). This audit substitutes **direct upstream-source comparison** (`git -C ruvnet/ruflo show origin/main:<path>`, ref **`367cb82ad`**) for every parity claim. No fork ADR premise was overturned by absence of the prover; where a 3.10.36 refinement matters it is an open question.

> **Corpus sweep:** subject-density grep over `docs/adr/ADR-*.md` for the 4 C7/C8 surfaces (market-data/OHLCV/candlestick; neural-trader/trading/backtest/portfolio-cg/attribution/signed; iot-cognitum/Cognitum/Seed/witness/fleet; plugin-creator/scaffold/marketplace/transfer) + `docs/upstream/INTEGRATION-LEDGER.md` (514 rows) C7/C8-grep. Fork source read-only via `git show c59d1b6c9`. **The neural-trader cluster is dense and exemplary** (ADR-0248 the honesty keystone, ADR-0251 the DA-dissent close, ADR-0252/0257 the ADR-126 `#2068` hand-port governance, ledger rows 168-173 the per-Phase picks). **market-data / plugin-creator / iot are thin** (one per-plugin ADR-0001 each + ADR-0248 description rewrites; iot adds the ADR-0079a aspirational keystone).

## The central divergence axis (read first — frames every premise)

C7/C8's structure is the **cleanest** of all categories audited and matches C5's "upstream-mirrored, no fork-originating regression" shape — but **even cleaner** (C5 had R1/R2 regressions; C7/C8 has zero). The four plugins map to four sub-patterns:

1. **neural-trader** — the only substantive runtime. Its fork-authored kernels (`src/sublinear-adapter`, `src/signed-attribution`, `src/signed-artifact`) are **upstream-authored (ADR-126 `#2068`, upstream `9c075a3c3`/`48cb0a7ee`/`11c1ad974`) and hand-ported** (ADR-0252→0257, ledger rows 171-173). Its plugin ADRs split into (a) the ADR-0001 contract (namespace audit — *already-compliant*, demonstrated), (b) the **ADR-0248/0251 description-honesty** rewrite (DEMONSTRATED premise — the "112+ MCP tools" overclaim; DA wanted DELETE, closed REWRITE by ADR-0251 after re-examination), and (c) the **ADR-126 Phase 1 namespace bug fix** (DEMONSTRATED — `trader-train/SKILL.md:28` wrote to undeclared `trading-models`; smoke asserted 4 namespaces not 5). All demonstrated; all sound. **The kernels are PARITY-via-handport, NOT fork-ahead.**

2. **market-data** — prose scaffold. Its only fork ADR is the per-plugin ADR-0001 (the recurring **`embeddings_embed`→`embeddings_generate` + namespace-to-`agentdb_*` → `memory_*`** bug-fix class) + the ADR-0248 description rewrite (drop "ingestion" framing). Premises DEMONSTRATED at the live-schema level (the `agentdb_pattern-store`/`agentdb_hierarchical-store` schemas have NO namespace param). The C2 pattern at its cleanest — over-demonstrated, shared-with-upstream fixes.

3. **iot-cognitum** — external-package + aspirational. ADR-0079a is the *proposed* aspirational keystone ("the agent-device control plane", Differentiation 10/10) whose runtime lives in the hardware-bound `@sparkleideas/plugin-iot-cognitum` (NOT-DRIVABLE). ADR-0001 + ADR-0248 (drop "hardware-bound" framing). Premises DEMONSTRATED (the namespace audit) or aspirational-proposed (ADR-0079a — design, not falsified). No fork-ahead capability the upstream mechanism lacked.

4. **plugin-creator** — meta-scaffolder. ADR-0001 (adopt-the-contract + scaffold-the-contract + the "19 controllers" myth-drop). DEMONSTRATED (the myth, the drift-table warnings — all verified accurate vs live schemas). The `transfer_plugin-*`/`transfer_store-*` intersection is real (allowed-tools) but those families are **upstream-authored** (the fabricated demo catalog is byte-identical upstream); the fork added no transfer capability.

**The C7/C8 risk class is: (a) ZERO fork-originating regression, (b) two narrow fork-ahead honesty fixes [ADR-0248/0251] — one incomplete (skips marketplace.json), (c) doc-drift mostly SHARED-with-upstream or fork-internal-cosmetic, (d) one ASSUMED-parity claim the swarm CAUGHT-AND-CORRECTED before landing [F-07-001 "ALIGNED-WITH-FORK"→"FORK-ONLY"].** This is the C2/C5 *over-demonstrated-premises* signature, NOT the C1 *wrong-shape-fabricated-brokenness* class.

## ADR premise classification — neural-trader keystones (the dense cluster)

| ADR | Surface touched | Problem statement | Class | Re-confirmed in C7/C8 env? / mistake notes |
|---|---|---|---|---|
| **ruflo-neural-trader ADR-0001** (plugin contract) | namespace audit; v3.6 pin; smoke-as-contract | 5 namespaces (`trading-strategies/-backtests/-risk/-analysis/-signals`) all kebab-case compliant; **"All access via `memory_*` — no bugs found"**; canonical 5-set per ADR-126 Phase 1 (was documented as 4 with a three-way README/ADR/skill mismatch — fixed in ADR-126 Phase 1) | **DEMONSTRATED (premise) — already-compliant** | The contract's namespace audit is correct. **Re-confirmed in C7/C8:** the 5 `trading-*` namespaces route via `memory_*` (which HAS a namespace param — verified at live schema); the `<plugin-stem>-<intent>` form is interpreted broadly (`trading-` not `neural-trader-`) — a recorded deliberate ergonomic choice. The three-way 4-vs-5 mismatch + the `trading-models` undeclared-namespace bug were REAL and fixed (see ADR-126 Phase 1 below). Sound. |
| **ADR-0248** §F-07-007 (marketplace description honesty) | `plugin.json` description | "112+ MCP tools" framing implies MCP-callable surface that does NOT exist (the 112 tools are via `npx neural-trader` CLI, not MCP from this plugin). 6 skills wrap an external CLI. **WARNING** (description-vs-implementation overclaim). | **DEMONSTRATED (file:line-grep'd; swarm-validated 5/6)** | Premise grep-verified (`plugin.json` description directly grep'd: "112+ MCP tools, swarm coordination, and portfolio optimization"). **Re-confirmed in C7/C8:** the live `plugin.json` carries the HONEST rewrite ("6 skills … wrap the external `npx neural-trader` CLI … does NOT expose neural-trader's CLI tools as MCP-callable") — the rewrite landed (`be780856f`). The lint §4 (`OVERCLAIM_DESCRIPTION_PATTERNS`) guards the per-plugin manifest. **Mistake note (DA-caught):** the rewrite is INCOMPLETE — `marketplace.json` still carries the (different) un-rewritten description, but it dropped "112+ MCP tools" too (so not lint-tripping). Sound at the per-plugin layer; D-CROSS-1 at the marketplace layer. |
| **ADR-0251** (close F-07-007 DA dissent on neural-trader) | the keep-vs-delete disposition | DA dissent (1/6): "re-examine whether description rewrite is sufficient OR whether the plugin should be DELETED from marketplace.json like F-07-001". | **DEMONSTRATED (re-examination) — close affirmative** | The re-examination is principled: verified (a) the rewrite is honest (grep'd `be780856f`), (b) the 6 skills compose real workflow value (external CLI + `memory_*` storage + `neural_train` — not a thin wrapper), (c) F-07-001≠F-07-007 (graph-intelligence was fork-only DOA + no implementation; neural-trader is upstream-authored + real underlying CLI + benchmarks/ test coverage). **Re-confirmed in C7/C8:** the plugin's `src/*` kernels are REAL, drivable, and sound (15/15 CG asserts, PageRank reproducibility, ed25519 roundtrip — `02-fork-diff`) — the strongest possible answer to the DA's "is this just docs?" concern. Option D lint reinforcement (5 overclaim phrases) landed (`05a700c`). Sound; DA mechanism worked as designed. |
| **ADR-126 Phase 1** (canonical 5-namespace alignment — upstream `#2068`, hand-ported) | `trader-train/SKILL.md` namespace; `smoke.sh` step 8 | `trader-train/SKILL.md:28` wrote to **undeclared `trading-models`** namespace; `smoke.sh` step 8 asserted **4 namespaces instead of 5** (three-way README/ADR/skill mismatch). **REAL BUG.** | **DEMONSTRATED (was-bug) → hand-ported** | Ledger rows 168-169: the original Batch-S pick was first mis-marked "superseded-by-adr" (ADR-0248/0251 excise the marketing claim only), then **re-disposed to "the namespace fix is a REAL BUG still in fork"** and hand-ported via `git cherry-pick -x 8d9e20f0c`→`7558f967b` (ADR-0257 HIGH-priority follow-on): `trading-models`→`trading-analysis`, fork-local `mcp__ruflo__` prefix preserved, smoke step 8 now asserts the 5-set and PASSes. **Re-confirmed in C7/C8:** live smoke step 8 asserts all 5 `trading-*` namespaces; the tree has 9 skills (matching the post-ADR-257 state). **Mistake-class (caught): superseded-vs-real-bug conflation** — row 168 nearly dropped a real bug as "superseded"; ADR-0257's C-D audit caught it. Sound after correction. |
| **ADR-126 Phases 2/3/4/6** (lifecycle / CG / signing / attribution — upstream `#2068`, hand-ported) | `src/sublinear-adapter.{ts,mjs}`, `src/signed-artifact.{ts,mjs}`, `src/signed-attribution.{ts,mjs}`, TTL/dedup on `trading-signals`/`trading-backtests` | Phase 2: ADR-125 memory lifecycle (24h TTL on signals, dedup-before-store on backtests, warm-HNSW doc). Phase 3: portfolio CG via `mcp__ruflo-sublinear__solve` (40-60× vs Neumann). Phase 4: Ed25519-signed backtest artifacts (CWE-347 key-pin). Phase 6: feature attribution via single-entry PageRank. **Upstream feature-adds, not bug fixes.** | **DEMONSTRATED (upstream-sourced) → hand-ported, all sound** | Ledger rows 170-173: each `git cherry-pick -x` from upstream (`d9bd4e6ad`/`9c075a3c3`/`48cb0a7ee`/`11c1ad974`), multi-conflict, brand-codemod `mcp__claude-flow__`→`mcp__ruflo__`, `@claude-flow/memory`→`@sparkleideas/memory`. **Re-confirmed in C7/C8 — DRIVEN END-TO-END:** CG parity `‖cg−neumann‖_∞<1e-4` HOLDS (1.6e-11…4.1e-11); PageRank reproducibility (same seed → byte-identical) HOLDS; ed25519 sign/verify + tamper-rejection + key-pin HOLD; the 3 committed smokes' runtime halves all ✓ (`02-fork-diff`). **Class: PARITY-via-handport — the fork ported upstream rather than inventing (exactly the null-hypothesis-prescribed behaviour).** Mistake-class: 0. Sound. **Note:** the 40-60× magnitude is upstream-native-WASM (`sublinear-time-solver@1.7.0`), correctly scoped; my JS-fallback measured 2-4× (D-NT-1, honest-attribution, not a defect). |

## ADR premise classification — market-data / iot / plugin-creator

| Plugin ADR | Premise | Class | Re-confirmed in C7/C8 env? / mistake notes |
|---|---|---|---|
| **ruflo-market-data ADR-0001** | THREE real skill bugs: (1) `market-ingest` referenced `embeddings_embed` (non-existent — real tool `embeddings_generate`); (2) `market-ingest` used `agentdb_hierarchical-store {namespace:'market-data'}` (routes by **tier**, namespace silently ignored); (3) `market-pattern` had the same hierarchical-recall + `agentdb_pattern-store {namespace:'market-patterns'}` bugs. All fixed → `memory_*` (namespace-routed). | **DEMONSTRATED (file:line) — exemplary** | **Re-confirmed at the live schema:** `embeddings_embed` absent / `embeddings_generate` present; `agentdb_pattern-store` schema = `{pattern,type,confidence}` (NO namespace); `agentdb_hierarchical-store` = `{key,value,tier(enum)}` (NO namespace); `memory_store` HAS `namespace`. The fix (switch namespaced reads/writes to `memory_*`) is the **exact correct remedy** — DEMONSTRATED structurally, not just asserted. Empirically: `agentdb_pattern-store` with a bogus type → `success:true, controller:archivist` (routes via ReasoningBank, ignores anything not in schema). **SHARED with upstream** (upstream skills had the same bug pre-fix). Sound. |
| **ruflo-iot-cognitum ADR-0001** | namespace audit (5 `iot-*` namespaces, **all already compliant**); v3.6 + Cognitum-firmware pin; 25 subcommands; 6 workers; 5-tier trust; smoke-as-contract. | **DEMONSTRATED (already-compliant)** | The namespace audit is correct (all kebab-case `<stem>-<intent>`). **Re-confirmed in C7/C8:** the substrate (5 `memory_*` namespaces) is reachable + RVF-durable; the hardware surface is NOT-DRIVABLE (no Seed). **Mistake note (DOC-DRIFT, D-IOT-1):** smoke step 5 asserts the FEDERATION tier names (`UNTRUSTED/VERIFIED/ATTESTED/TRUSTED/PRIVILEGED`) not the iot model's own (`UNKNOWN/…/FLEET_TRUSTED`) — passes only because the federation tiers appear in the cross-ref paragraph. Cosmetic. Sound contract; mis-targeted smoke assertion. |
| **ruflo-iot-cognitum ADR-0079a** (aspirational keystone) | single-installation Claude Flow lacks a physical-device control plane; proposes `@claude-flow/plugin-iot-cognitum` (the Cognitum Seed bridge: device-agent duality, 5-tier trust, telemetry anomaly, OTA orchestration, edge-cloud federation). **`status: proposed`** — aspirational design (Differentiation 10/10 self-scored). | **DEMONSTRATED (gap) — ASPIRATIONAL, partially realized, NOT falsified** | The *gap* (no agent-native device fleet) is real. The *design* is the "fleet-as-swarm" vision. **Re-confirmed in C7/C8 — the realized surface is the external package + substrate; the device data-plane is NOT-DRIVABLE (no hardware) = SYMMETRIC with any prover.** Not a wrong premise — an aspirational `proposed` ADR whose hardware data-plane is unverified-by-safety-scope, not falsified (same shape as C5's ADR-0078a federation keystone). Sound. |
| **ruflo-plugin-creator ADR-0001** | adopt-the-contract (this plugin) + scaffold-the-contract (output) + drop the "19 AgentDB controllers" myth (real: ~15 `agentdb_*` MCP tools, 29 ControllerName). The scaffolder warns about 4 real MCP-tool bugs (`embeddings_embed`, `agentdb_hierarchical-*`/`agentdb_pattern-*` namespace, `pattern`-vs-`patterns`). | **DEMONSTRATED (the myth + the 4 warnings are factually accurate)** | **Re-confirmed in C7/C8:** all 4 drift-warnings are correct vs the live schemas (verified). The "19 controllers" myth-drop is real (smoke step 5 regression-checks its absence). **Mistake note (DOC-DRIFT, D-CREATOR-1):** `validate-plugin` checks 3-5 ("each skill in `plugin.json.skills`") contradict `create-plugin` step 4 ("do NOT include skills/commands/agents arrays — Claude Code auto-discovers"); validate-plugin would no-op those 3 checks on a correctly-scaffolded plugin. SHARED (upstream same). Sound scaffolder; internal validate/create tension. |

## Upstream-merge program ADRs (the C7/C8 port governance)

| ADR | Premise | Class | Notes |
|---|---|---|---|
| **ADR-0248** (plugin marketplace integrity + honesty, CT-O) | 9 slice-07 findings: 2 CRITICAL DOA/phantom (F-07-001 graph-intelligence, F-07-002 rabitq) + description-honesty (F-07-006 iot/market/kg + F-07-007 neural-trader) + brand-drift (F-07-004) + NOTEs. Per-finding stub-honesty triage + a marketplace-integrity lint. | **DEMONSTRATED (file:line per cluster; 6-expert swarm) — but with 3 CAUGHT mistakes** | The hand-port + honesty governance for ALL the C7/C8 description rewrites. **Re-confirmed in C7/C8:** the per-plugin `plugin.json` rewrites landed (iot/market/neural-trader honest); the lint shipped (4 assertions). **Mistake-catalogue (swarm-CAUGHT before landing — see §Mistake catalogue M-C7-1/2/3):** (a) F-07-001 "ALIGNED-WITH-FORK" was WRONG (graph-intelligence is FORK-ONLY — `ls ruvnet/ruflo/plugins/` has no such dir); (b) F-07-004 "ALIGNED-WITH-FORK" was WRONG (upstream already SOLVED it with a shim; fork was the regressed copy); (c) the "5 occurrences" count was wrong (`grep -c`=3). All three corrected by the swarm review. Sound after correction. |
| **ADR-0251** (close F-07-007 DA dissent) | (see neural-trader keystones) | DEMONSTRATED | Close affirmative; DA mechanism worked. |
| **ADR-0252** (Batch S source-conflict re-disposition) | 19 deferred upstream commits incl. **5 ADR-126 neural-trader Phases**; per-family disposition: 5 supersede (the SKILL.md surface the fork rewrote) / 3 pull-pending / 2 defer / 9 defer-per-SHA. | **DEMONSTRATED (governance)** | The disposition that routed the ADR-126 Phases. **Mistake-class (caught by ADR-0257): the "5 supersede" was over-broad** — it superseded the SKILL.md *marketing surface* but row 168's ADR-0257 audit found the Phase 1 *namespace fix* (and Phases 2-6 *substrate code*) were REAL and NOT covered → hand-ported separately (rows 169-173). Sound after the ADR-0257 correction. |
| **ADR-0257** (backlog execution plan) | Item #9: "5 ADR-126 superseded-by-adr rows — never spot-checked whether ADR-0248/0251 cover the same surface" → C-D audit. Items #10-11: fork-native memory_export + plugin-bridge (NOT C7/C8). | **DEMONSTRATED (audit governance)** | The audit that caught the ADR-0252 over-supersede and hand-ported the real ADR-126 Phases (rows 169-173). This is the **paired-fix completion** the C5/C3 audits prize — the fork eventually carried the upstream bug fix it had nearly dropped. Sound. |

## Mistake catalogue (ADR-0291 §Confirmation taxonomy + C2 *diverged-since* + C4/C5 *doc-drift*)

Searched every C7/C8 ADR premise + the live plugin prose for the failure modes. **Finding: the C7/C8 fork ADRs are strongly evidence-disciplined** — ADR-0001s are file:line-cited; ADR-0248 greps upstream per cluster; ADR-126 picks are `git cherry-pick -x` trailer-greppable; ADR-0251 re-verifies by inspection; ADR-0252/0257 self-correct. **No C7/C8 ADR was built on a fabricated/un-probed brokenness premise.** The catalogue is (a) the swarm-CAUGHT ASSUMED-parity claims (M-C7-1/2 — corrected before landing), (b) the superseded-vs-real-bug conflation (M-C7-3 — caught by ADR-0257), and (c) **doc-drift, mostly SHARED-with-upstream or fork-internal-cosmetic** (M-C7-4..8).

| # | Mistake type | Where | Severity | Detail |
|---|---|---|---|---|
| **M-C7-1** | **ASSUMED upstream-parity, CAUGHT-AND-CORRECTED by the swarm: "ALIGNED-WITH-FORK" for graph-intelligence** | ADR-0248 Cluster A check #2 (pre-review) | (caught — no ship impact) | The ADR asserted `ruvnet/ruflo` ships `plugins/ruflo-graph-intelligence/`; the 6-expert swarm review verified `ls ruvnet/ruflo/plugins/` returns 33 dirs, **none** named that — the plugin is **FORK-ONLY**. Disposition (DELETE) unchanged but the divergence-rationale was rewritten ("deleting a fork-only DOA plugin is strictly less divergent"). **This is the ADR-0291 *assumed-not-verified* class — but caught by the review process before landing** (the counter-process working as designed). Not a C7/C8-plugin defect (graph-intelligence is its own deleted plugin); recorded because the DELETE precedent frames the neural-trader keep-vs-delete decision. |
| **M-C7-2** | **ASSUMED upstream-parity, CAUGHT-AND-CORRECTED: "ALIGNED-WITH-FORK" for F-07-004 hooks brand** | ADR-0248 Cluster C check #2 (pre-review) | (caught — no ship impact) | The ADR claimed upstream `ruflo-core/hooks/hooks.json` carries the same `claude-flow@alpha`; the swarm verified upstream = **0** hits (upstream SOLVED it with a `ruflo-hook.sh` shim; the fork was the stale/regressed copy). Disposition upgraded from substitution to **import-upstream's-shim** (`cd5dd216e`, byte-identical re-import). Same *assumed-parity* class, caught + corrected. (ruflo-core = C3 surface, not C7/C8 — recorded as the sibling discipline.) |
| **M-C7-3** | **Superseded-vs-real-bug conflation (diverged-since), CAUGHT by ADR-0257: ADR-126 Phase 1 namespace fix nearly dropped as "superseded"** | ledger row 168; ADR-0252 "5 supersede" | (caught — fix landed) | ADR-0252 marked the 5 ADR-126 neural-trader Phases "superseded-by-adr" (ADR-0248/0251 rewrote the SKILL.md surface). But ADR-0257's C-D audit found the Phase 1 *namespace fix* (`trading-models` undeclared) + Phases 2-6 *substrate code* were REAL and NOT covered by the description rewrite → hand-ported separately (rows 169-173). **Class: demonstrated-then-but-the-real-bug-was-masked-by-an-over-broad-supersede** — the C2 *diverged-since* failure mode's inverse (here the fork nearly *failed to carry* an upstream fix). Caught + remediated. Sound. |
| **M-C7-4** | **Doc-drift (SHARED + LINT-SCOPE GAP): marketplace.json carries the overclaim the per-plugin rewrite removed** | `.claude-plugin/marketplace.json` (iot "witness chain verification for Cognitum Seed hardware", market "ingestion"); lint `plugin-marketplace-integrity.test.mjs:86,110,235` | LOW (material to honesty program) | The ADR-0248 F-07-006/007 rewrite edited `plugins/*/.claude-plugin/plugin.json` (honest) but NOT the top-level `.claude-plugin/marketplace.json` (still over-claiming). The marketplace.json iot description is the **exact string in the lint's own `OVERCLAIM_DESCRIPTION_PATTERNS`** (line 86) — but Assertion 4 walks only per-plugin manifests (`walkPluginManifests()`, line 110), never marketplace.json, so the forbidden phrase lives unlinted at the install-decision surface the lint's own message names. **SHARED with upstream** (upstream marketplace.json byte-identical at `367cb82ad`). = `02-fork-diff` D-CROSS-1. Fix: extend Assertion 4 to walk marketplace.json + apply the rewrite there. → C7/C8 disposition (queen's call). |
| **M-C7-5** | **Doc-drift (SHARED, = ADR-0293-D2-class but INHERITED): transfer_* families return a fabricated demo catalog** | `v3/@claude-flow/cli/src/plugins/store/discovery.ts:247/253-256/288-289/1046-1055` (+ `transfer/store/discovery.ts` mirror) | LOW (honest-gate concern) | `transfer_plugin-official`/`-featured` + `transfer_store-trending`/`-featured` return a HARDCODED seed catalog with placeholder CIDs (`bafybeiiotcognitumplugin2026`), `totalDownloads:50000`, `reputation:100`, placeholder signatures (`ed25519:genesis-pattern-signature`), `lastUpdated:now`. **BYTE-IDENTICAL in upstream** (`367cb82ad` discovery.ts) → **SHARED, NOT fork-introduced** (the fork only codemod-rebrands the `@claude-flow/`→`@sparkleideas/` IDs). Same *class* as ADR-0293 D2 (hooks_transfer demo-data) but inherited. **The one path plugin-creator uses (`transfer_plugin-search` name-lookup) is HONEST** (returns `{total:0}` for an untaken name). = `02-fork-diff` D-TRANSFER-1. Disposition: cross-ref ADR-0293 D2; the inherited-vs-fork distinction matters for whether to fix fork-side or wait for upstream. |
| **M-C7-6** | **Doc-drift (fork-internal, stale-smoke): all 4 plugin smokes check 1 hardcodes `0.2.0`** | `scripts/smoke.sh` check 1, all 4 plugins (live versions 0.2.24/0.2.27/0.2.24/0.2.28) | LOW | Check 1 hardcodes `expected 0.2.0` but plugins are past it; the `@claude-flow/cli v3.6` README-pin greps the literal (passes) but live cli is `3.7.0-alpha.10-patch.415`. Substance (skills/agent/command present, namespace, ADR Proposed, no wildcards) PASS. Same class as C4/C5 stale-smoke (M8). = `02-fork-diff` D-CROSS-2. |
| **M-C7-7** | **Doc-drift (doc-vs-live pin): neural-trader README pins `@sparkleideas/memory@3.0.0-alpha.18`, live env ships `alpha.15-patch.415`** | neural-trader README §"Memory lifecycle (ADR-125)" | LOW | The README/ADR prose pins a higher memory alpha (alpha.18 — warm-HNSW/RRF+MMR/TTL/dedup per ADR-125 Phases 3-5) than the env resolves (`alpha.15-patch.415`). The lifecycle features may differ from the prose. = `02-fork-diff` D-NT-2. |
| **M-C7-8** | **Doc-drift (SHARED, fork-internal): plugin-creator validate/create internal inconsistency + iot smoke wrong tier-names** | `validate-plugin/SKILL.md` checks 3-5 vs `create-plugin` step 4; iot `smoke.sh` step 5 | LOW | (a) validate-plugin checks "each skill in `plugin.json.skills`" but create-plugin (correctly) tells authors NOT to emit those arrays (auto-discovered) → those 3 checks no-op on a correctly-scaffolded plugin. (b) iot smoke step 5 asserts FEDERATION tier names not iot's own. Both cosmetic; SHARED. = `02-fork-diff` D-CREATOR-1 + D-IOT-1. |

**Crucially — NO C7/C8 fork ADR was built on a fabricated/un-probed brokenness premise.** Every keystone's problem statement was demonstrated at authoring time (ADR-0001s file:line-cite the skill bugs; ADR-0248 greps upstream per cluster; ADR-126 picks are `cherry-pick -x`; ADR-0251 re-verifies by inspection). The mistakes are (a) two **ASSUMED-parity claims the 6-expert swarm CAUGHT-AND-CORRECTED before landing** (M-C7-1/2 — the anti-misdiagnosis counter-process working exactly as ADR-0292 intends), (b) one **superseded-vs-real-bug conflation caught by ADR-0257** (M-C7-3 — the fork eventually carried the upstream fix it nearly dropped), and (c) **doc-drift** (M-C7-4..8) — **mostly SHARED-with-upstream or fork-internal-cosmetic**. This is the **C2/C5 pattern** (over-demonstrated premises; risk = shared-doc-drift + incomplete-honesty-rewrite), NOT the C1 F1 *wrong-shape-fabricated-brokenness* class.

## Summary statistics

- **ADRs deep-audited: 11** — neural-trader: ADR-0001 contract + ADR-0248 §F-07-007 + ADR-0251 (DA close) + ADR-126 Phases 1-6 (counted as the hand-port cluster); market-data: ADR-0001; iot: ADR-0001 + ADR-0079a (aspirational keystone); plugin-creator: ADR-0001; program: ADR-0248 (CT-O) + ADR-0252 + ADR-0257 (overlapping count).
- **Problem statement DEMONSTRATED: 11 / 11 (100%).** Every audited C7/C8 fork ADR's premise was evidence-backed at authoring time (file:line citations in the ADR-0001s, `git show origin/main` parity greps in ADR-0248, `cherry-pick -x` trailers for ADR-126, inspection re-verification in ADR-0251, self-correcting audits in ADR-0252/0257). DEMONSTRATED via live-schema re-confirmation in the C7/C8 env for the namespace-routing class; DRIVEN END-TO-END for the neural-trader kernels.
- **ASSUMED-brokenness premises: 0.** No C7/C8 ADR fabricated or un-probed its problem statement. (ADR-0079a is *aspirational* `proposed` design — its hardware data-plane is unverified-by-safety-scope, not assumed-broken. ADR-0248's two "ALIGNED-WITH-FORK" claims were ASSUMED-parity but **caught + corrected by the swarm before landing**, not shipped.)
- **DEMONSTRATED-then-but-DIVERGED-SINCE / doc-drift issues: 8** — M-C7-1 assumed-parity-caught (graph-intelligence FORK-ONLY) [caught, no-ship], M-C7-2 assumed-parity-caught (hooks shim) [caught], M-C7-3 superseded-vs-real-bug-caught (ADR-126 Phase 1) [caught + landed], M-C7-4 marketplace.json overclaim + lint-scope gap [LOW, SHARED, material], M-C7-5 transfer-catalog fabrication [LOW, SHARED, = ADR-0293-D2-class inherited], M-C7-6 all-4 stale-smoke [LOW, fork-internal], M-C7-7 neural-trader memory doc-pin [LOW], M-C7-8 validate/create + iot-tier cosmetic [LOW, SHARED]. The re-convergence/cleanup targets — **3 already self-caught/remediated, 5 are LOW shared/cosmetic doc-drift.**
- **FORK-AHEAD that closes an upstream drift: 1 (incomplete) + 1 gate** — the per-plugin `plugin.json` honest-description rewrites (ADR-0248/0251) CLOSE upstream's overclaim at the per-plugin layer (genuine fork-ahead; upstream carries the overclaim) — **but incompletely (skips marketplace.json, M-C7-4)**; plus the marketplace-integrity lint (fork-only gate). Both KEEP-with-justification.
- **Mistake-class distribution (ADR-0291 taxonomy):** wrong-shape-fabricated-brokenness **0** · counters-vs-content **0** · dev-node_modules inspection **0** · stale-checkout reads **0** (ADRs use `git show origin/main`; I pinned `c59d1b6c9`) · masked-entry-point **0** (no bin to trace — the kernels are imported `.mjs`; the substrate is the central MCP server). The **C7/C8-dominant classes are: (a) ASSUMED-upstream-parity-CAUGHT-BY-THE-SWARM** (M-C7-1/2 — the counter-process working) **+ (b) doc-drift mostly SHARED-with-upstream** (M-C7-4..8) **+ (c) one incomplete-honesty-rewrite** (M-C7-4) — distinct from C1's *wrong-shape*, C4's *ratified-decision-not-propagated*, and even cleaner than C5 (which had real R1/R2 regressions; C7/C8 has ZERO fork-originating regression).
- **UPSTREAM-BROKEN: 0 confirmed** (consistent with the null hypothesis). **0 candidates** — no full-bar runtime breakage on any C7/C8 surface. (The transfer-catalog fabrication self-images rather than crashes; it is SHARED-honesty-debt, not a named runtime breakage.)
- **NOT-DRIVABLE (out of safety scope): 1 plugin** — iot-cognitum hardware surface (no Cognitum Seed; SYMMETRIC with any prover). Classified on substrate (PARITY) + the aspirational ADR-0079a keystone (not falsified) + the marketplace honesty.

## Open items routed to the prover / devil's advocate

1. **M-C7-5 / D-TRANSFER-1 — transfer-catalog fabrication at live 3.10.36.** Confirmed byte-identical in upstream source `367cb82ad`. Does live 3.10.36 `transfer_plugin-official`/`transfer_store-trending` return the same fabricated demo data? (Confirms SHARED at 3.10.36 vs an upstream real-registry wiring the fork missed.) Low stakes — the plugin-creator path used (name-search) is honest.
2. **M-C7-4 / D-CROSS-1 — marketplace.json honesty at 3.10.36.** Upstream marketplace.json (`367cb82ad`) carries the byte-identical iot overclaim. Does live 3.10.36 still ship `"witness chain verification for Cognitum Seed hardware"` in marketplace.json? **Disposition recommendation regardless:** extend the marketplace-integrity lint Assertion 4 to walk `.claude-plugin/marketplace.json` descriptions AND apply the F-07-006/007 rewrite there (the lint's own message says marketplace.json is the install-decision surface — it should look there).
3. **neural-trader kernel re-confirmation (DA).** The kernels are the only C7/C8 surface with falsifiable numerical claims. Recommend the DA independently re-run `/tmp/c78-evidence/fork/drives/cg-parity-driver.mjs` + the 3 committed smokes (`smoke-neural-trader-{portfolio-cg,feature-attribution,backtest-signing}.mjs`) with the `skills/` tree materialized so the `[2/3]`/`[3/3]` SKILL.md call-site halves also exercise — to cross-confirm the parity-guarantee + reproducibility + key-pin findings I drove.
4. **neural-trader native-WASM speedup arm (D-NT-1).** The 40-60× CG / native-PageRank claim is attributable only to `mcp__ruflo-sublinear__solve` (the `ruflo-sublinear` plugin, not registered). If a prover can register it, re-run the bench to confirm the native magnitude (the fallback 2-4× is what I measured, honestly scoped). Low stakes — the fallback self-discloses `path:'cg-local'`.
5. **iot-cognitum data-plane — SYMMETRIC coverage gap.** The Cognitum Seed hardware surface (device register/fleet/firmware/witness over a real Seed) is undriven by safety rule. A verdict needs a physical Seed + `@cognitum-one/sdk` (out of scope). Do NOT attempt. The substrate (5 `memory_*` namespaces) is PARITY; ADR-0079a is aspirational-not-falsified.
